# Agent leave-off (read this first)

**This file is the SoT for “where did we stop?”**  
Chat transcripts are not. A later Grok / Cursor cloud agent will not see a prior chat unless the work and this stamp are on GitHub.

| Field | Value |
|-------|--------|
| Updated | 2026-09-10 21:31 EDT (CoS closeout on `cursor/honer-15m-sibling` #178: Lab invented `CLOCK-HOUR-WRAP` as farm notebook `F-CLOCK-HOUR-WRAP-0-45` skip `:00`/`:45`, rate 0.5, execution false; chair untouched. Unused legal kinds empty. I still hungers invent-next-kind (no exhausted file) — next CoS fire. Hour-close still executing. Consult off. Trading NOT ARMED. next=idle) |
| Interim operator | Cursor chat (Grok bot usage exhausted until **2026-09-13**) |
| Repo SoT | **Live work is PR [#178](https://github.com/swellbear/gated-formalization/pull/178)** (`cursor/honer-15m-sibling`). [#176](https://github.com/swellbear/gated-formalization/pull/176) is still OPEN, not merged (`cursor/part-a-clerical-trust-boundary`). `origin/master` is still `b063f56` (#175). Do not branch from `origin/master`. |
| Local Windows tree for this interim | `C:\Users\bearh\gated-formalization-master-hub` |
| Do **not** treat as SoT | `C:\Users\bearh\gated-formalization` on `cursor/eia-window-job2` (stale + dirty) |
| Active track | `learning_lane_15m` (KXBTC15M paper gym) + Phase 1 desktop hub + `honer_15m` discovery organ (own root; consult into factory `decide()` is **off**) |
| Crew | `docs/agents/` — CoS routes; desk is live board |
| Trading | **NOT ARMED** |

## Required closeout (every session)

Before stopping, the agent that just worked **must**:

1. Update this file **and** `docs/agents/DESK.md` so they match **committed** truth.
2. Commit this file with the work (or a docs-only follow-up if the work already landed).
3. Push the branch / open or update the PR so `master` (or the open PR) is what the next bot clones.

If it is not in git on GitHub, the next bot does not have it.

## What is on `origin/master` versus what is only on this branch

**`origin/master` is the public SoT.** GitHub Pages serves that commit only. A local export, a desktop hub, or an unmerged PR is **not** published.

`origin/master` before this fold is `b063f56` (PR #175 — live digest refresh + hash-stamped validator report, validator still judicial). `#174` hired Soften Critic and drafted the bar. `#173` put autostart scripts on the tree, not registered. GitHub Pages is **not** self-maintaining. A local export is not a publish.

**On `master` now:** learning wake, digest, park file with RUN-ONLY / classes, Lab PROPOSED 01 (RUN-ONLY, not admitted), export guard, serve-on-proof runner, kill file, continuous PaperWatch invoke, human artifact-proof.

**Armed locally:** `golf-offshoot/data/learning_lane_15m/latest/RUNNER_ARMED` and `PUBLISH_ARMED` (both gitignored). The runner still does not commit or push. Gym Pages updates are the `PUBLISH_ARMED` hook (hub allowlist via a master worktree). Never sibling-HEAD-to-master. Cloud CoS/worker do not publish.

Do not write branch state as if it were landed. If Pages `generated_at` is more than one 15-minute window behind now, the public page should say so itself.

## What is already on master (PR #163 and earlier)

Desktop hub (control surface):

- Launch: `golf-offshoot/scripts/windows/Open-Phase1-Hub.bat` → `python -m golf_offshoot shell` → `http://127.0.0.1:8765`
- Code: `golf-offshoot/src/golf_offshoot/operator_surface/app.py`
- Two lanes, exact selector `lane=golf|learning_lane_15m` (bare `15m` is unknown → golf)
- Buttons: ingest / live / shadow / loop / refresh. Cash / arm / Kalshi-auth actions are refused.
- Golf live/loop auto-applies **paper** advises only. 15m live/loop = public fetch → paper autobet → settle join.
- ntfy: one ping on operator-triggered ingest/live/loop finish if `NTFY_TOPIC` is set.
- Auto-reload: artifacts soft-refresh; git/code change re-execs the hub. Watched code is `operator_surface/*.py`, `learning_lane_15m/*.py`, `__main__.py`, `audit/shadow_settle.py`. `git_tip` resolves branch SHAs through the worktree `commondir` — without that a linked worktree never sees a commit on its own branch.

15-minute Kalshi learning lane:

- Docs: `golf-offshoot/docs/LEARNING_LANE_15M.md`
- Code: `golf-offshoot/src/golf_offshoot/learning_lane_15m/`
- CLI: `python -m golf_offshoot lane-15m`
- Public read only. Official settle = Kalshi `result` matched to CF Benchmarks `BRTI`.
- Landed: PRs #151, #153, #156–#159, #161, #162.

Windows path fix (this branch): settlement filenames sanitize `:` (`safe_artifact_stem`). `window_id` inside JSON still keeps colons. Export keeps a published 15m settle history when the local journal would drop `Settled windows` / `paper_win`.

Golf Phase 1 (do not reopen unless asked):

- Stamp: `golf-offshoot/docs/phase1_dryrun/OPERATOR_STATUS_STAMP.md`
- WC1 admitted FAIL / park unproven. WC2 parked/rejected. Edge **not** established. Idle on WC3+ until a new settled week.
- **Golf idle is ON** and nothing on the 15m lane clears it, retunes golf θ, rewrites that stamp, or reopens WC1 / WC2 / WC3+.

## Learning is now **on** (this is the change since the last leave-off)

> **Public hub = `origin/master`.** A local `observability-export` does not update Pages. After PR #165 merges, `master` carries the learning wake (`learn-15m`), the SOURCE digest, the method park, Lab PROPOSED 01 (PARKED, not admitted), the export-time pending/settled guard, and the corrected `071500-00` missing-paper-join wording. Clone `master` after that merge. If you still see "Live 1500 stays SETTLE_PENDING" on GitHub Pages, the deploy has not finished — that wording is a known stale falsehood and must not be treated as current.

PaperWatch alone was never learning. Two halves now run together:

1. **PaperWatch** — the researcher/export half, one cycle roughly every **90 seconds** (`DEFAULT_INTERVAL_S = 90.0`, `golf-offshoot/src/golf_offshoot/learning_lane_15m/watch.py`). Public fetch → paper autobet → settle join → export. It starts itself with the 15m hub.
2. **The learning wake** — the crew half. It scans settlements / paper / journal / manifest, raises `new_settle` / `new_fill` / `pending_cleared` events, and **names which roles are owed a turn.**

Run the tick:

```powershell
cd golf-offshoot
$env:PYTHONPATH = "src"
python -m golf_offshoot learn-15m          # human-readable
python -m golf_offshoot learn-15m --json   # same tick as JSON
```

Wake state file: `golf-offshoot/data/learning_lane_15m/latest/learning_wake.json` (gitignored, derived, **never authoritative** — the files on disk are).

**Python names owed roles. It never marks one served, never writes a desk thread line, never Softens, never ADMITs, and never invents a win, a lose or a pnl.** A role clears its own line only after it really ran:

```powershell
python -c "from golf_offshoot.learning_lane_15m.learn import mark_roles_served; mark_roles_served(['operator'], by='operator', note='what you actually did')"
```

Read these two before touching the lane:

- **SOURCE honesty digest (living spine, every claim cited to a file):** `golf-offshoot/docs/LEARNING_LANE_15M_SOURCE_DIGEST.md`
- **Operator method park (residuals + explicit reopen triggers):** `golf-offshoot/docs/LEARNING_LANE_15M_METHOD_PARK.md`
- Standing conflict flag: `golf-offshoot/docs/LEARNING_LANE_15M_SOURCE_CONFLICT.md`

## Published hub snapshot — corrected against the digest and live files

**These numbers move.** The loop is live, so a window settles and the book changes roughly every 15 minutes. Figures below are as of **2026-09-07 18:19 EDT**. Do not trust them as current — re-read them with `learn-15m` and from `golf-offshoot/data/learning_lane_15m/`.

**Two paper lineages exist. They are two separate books, not two views of one book. Never sum them.**

*Lineage A — the local paper book on this tree* (`golf-offshoot/data/learning_lane_15m/paper/` + `paper/ledger.json`):

- Seed `starting_bankroll` 100.00 → `bankroll` **95.59** · `betting_pnl` **-4.41** · `deposits` 0.00 · `withdrawals` 0.00 · 24 entries · 11 settled events
- 12 settle files and 12 paper books on disk: **11 joined and settled, 1 open**
- Windows joined run `KXBTC15M-26SEP071545-45` onward. Each row's pnl comes off its own `paper/<stem>.json` `settlement_pnl` — e.g. `071600-00` is `+0.42` (the previous leave-off's `$+0.44` was wrong), `071615-15` is `-1.00`
- The recorded pnl figures reconcile the `bankroll_before` → `bankroll_after` chain in `ledger.json`. Nothing is recomputed or averaged

*Lineage B — the published Pages export* (`docs/observability-hub/data/manifest.json` `$.lanes[1]`):

- `KXBTC15M-26SEP071445-45` — official `result=yes`, **paper_win**, paper pnl `+1.67`. **Kept as published history.** The book that produced it is not on this tree, so `+1.67` is never re-derived here and never added into lineage A
- Why they are split, cited to code: `learning_lane_15m/paths.py` `artifact_root_15m()` prefers `/workspace/kalshi_15m_exports` and falls back to the repo root when `/workspace` is absent, as it is on this Windows tree. Same code, different machine, different book

**The two residual states that look alike on a dashboard and are not alike:**

- **Pending for want of a Kalshi result** — a true pending window. Kalshi has not spoken; the paper book here is open. There is normally exactly one, and it **rotates every ~15 minutes** — it was `071815-15` at 18:03 EDT, `071830-30` at 18:20 and `071845-45` at 18:32. **Read the current one off `learn-15m`, never off this file.** A ticker that has left this state did not fail; it settled, and its pnl is on its own book
- **`KXBTC15M-26SEP071500-00` is a missing paper join, NOT a pending window.** Kalshi settled it **`yes`** (`latest/journal.json`, `status` `finalized`). The paper book the published lineage names is **not on this tree**, so there is **no paper pnl here and none is invented.** `SETTLE_PENDING` is the wrong banner — it says "Kalshi has not spoken," and Kalshi has. `result=yes` does not license a win, a `+pnl`, a `0`, or a loss here. A window with no book has no pnl; that is a true statement, not a missing number

**Still stale on the published surface (owned by `systems`, not by whoever reads this):** `manifest.json` still carries *"SETTLE_PENDING until Kalshi result"* for `071500-00` at `$.lanes[1].settle.headline`, `$.lanes[1].settle.residual[0].note`, `$.lanes[1].last_run.headline` and `$.lanes[1].last_run.notes[2]` — while the **same file** already states the honest version at `$.lanes[1].settle.counts[5].note`. That re-word is Systems' job.

**Unmeasured tape:** a handful of windows are `finalized` with an official result in `latest/journal.json` but have no settle file and no paper book anywhere on this tree — they closed before the local book seeded at 15:42:37 EDT. The digest named 8 of them at 18:03 EDT; `071315-15` has already rolled off, leaving `071330-30`, `071345-45`, `071400-00`, `071415-15`, `071430-30`, `071515-15`, `071530-30` at 18:28 EDT. **`latest/journal.json` is a rolling 21-window tape, not an archive** — the oldest window falls off as a new one opens, so this list shrinks from the front. A shrinking list means windows are being *forgotten*, not resolved. **Unmeasured is not lost and not losses.** Do not backfill them from their results and do not count them in a denominator.

**15m charts now exist:** `docs/observability-hub/data/charts/learning_lane_15m/paper_window_strip.png` is a real, labeled board rendered from real files, drawing lineage A apart from lineage B with a *never summed* note. It is **behind**, not invented — its header stamps `journal generated_at=2026-09-07T17:10:08`, and its `071500-00` cell still prints `SETTLE_PENDING`. The old "charts not yet available" leftover is stale.

**No edge, no track record.** Every fill is taken at the posted mark with `entry_edge=0.0`; the model is the market. Paper fills are not admits (`lab_admits=false`). This lane has no dated record — `manifest.json` `$.lanes[1].records` is `[]`.

Golf WC1 FAIL does **not** transfer into this lane, and nothing on this lane retunes golf θ.

## Next (safe, in order)

**Start every turn by running the tick.** `python -m golf_offshoot learn-15m` tells you which roles are owed and why. Serve them in the Protocol learning-tick order below; do not ping Founder to approve any of it.

1. **The learning tick order is `lane-15m` → `digest-figures` → `digestor` (caveats only) → `operator` → `systems` → `validator`.** `hub-ui` runs only if the display is wrong or stale. **`illustrator` is owed by the wake** when the PNG lags live journal/settlements by more than one window (one window of trail is the open window and is allowed). **`lab` PROPOSED 02 landed 2026-09-08 16:55 ET** — `R-SKIP-2TO1-FAVORITE` declared (`favorite_odds=2`, skip posted YES ≥ 2/3), not a revival of `R-SKIP-COINFLIP`. **Operator 17:11 ET RUN-ONLY**; `execution=true` (paper only); not scored; `R-SKIP-COINFLIP` untouched. Artifact: `LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_02.md`. **Soften Critic 17:40 ET CRITIC 03** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_03.md`): six UPHELD on Turn 3 bar hashes + the executing registry row; bar not edited; not scored. **Operator 17:42 ET ANSWER 03** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_03.md`): six SUSTAINED; bar names `R-SKIP-2TO1-FAVORITE`; `favorite_odds=2` recorded `verifiably_preregistered: false`; condition 1 unmet on the amended bytes; not scored; not bound. **Soften Critic 17:57 ET CRITIC 04** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_04.md`): five UPHELD on the ANSWER 03 hashes; bar not edited; not scored. **Operator 18:20 ET ANSWER 04** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_04.md`): five SUSTAINED; next-look reject 0.504 printed; failing_set struck as other bytes; conventional-prior `dies_if` removed; `0a480d4` live-tip claim dropped; lived/replay labeled prose-only; condition 1 unmet on the amended bytes; not scored; not bound. **Soften Critic 18:28 ET CRITIC 05** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_05.md`): two UPHELD on the ANSWER 04 hashes (`D9B484FD…` / `48EAE5D1…` / `1DDD3CCE…`); L2 α assumes an L1-score increment `record_trial` cannot perform; X2 still says the rule is executing with no process cited; bar not edited; not scored; not bound. **Operator 18:45 ET ANSWER 05** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_05.md`): two SUSTAINED; L2 α struck "after L1's increment"; L2 shares next-look set (0.504 / 0.2243 / 0.504 / 0.583); clause (2) increments after an L2 look only; X2 says registry `execution` true and lived honoring unproven this turn (no tip, no cited decision row); condition 1 unmet on the amended bytes; not scored; not bound. **Soften Critic 18:56 ET CRITIC 06** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_06.md`): one UPHELD on the ANSWER 05 hashes (`E2E9FC40…` / `471A1A16…` / `1DDD3CCE…`); clause (2) "L2 look only" forbids the declaration increment that produces k=2 / reject 0.504; bar not edited; not scored; not bound. **CoS 19:00 ET assigned operator** to answer that one. **Operator 19:12 ET ANSWER 06** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_06.md`): one SUSTAINED; clause (2) increments on declaration and after an L2 look; "L2 look only" struck as the increment rule; L2 table dropped the deleted-wording quote; condition 1 unmet on the amended bytes; not scored; not bound. **CoS 19:30 ET assigned soften-critic** to attack those new hashes. **Soften Critic 19:45 ET CRITIC 07** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_07.md`): one UPHELD on the ANSWER 06 hashes (`9F72AF66…` / `726FF992…` / `1DDD3CCE…`); clause (2) increment names declaration + L2, but the same clause still calls α_1 = 0.025 the "First look"; bar not edited; not scored; not bound. **CoS 19:45 ET assigned operator** to answer that one. **Operator 19:52 ET ANSWER 07** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_07.md`): one SUSTAINED; clause (2) labels α_1 the schedule's first term, spent on declaration, historical; first scoring look is k=2 / α=0.008333; "First look" struck as a name for α_1; condition 1 unmet on the amended bytes; not scored; not bound. **CoS 20:00 ET assigned soften-critic** to attack those new hashes. **Soften Critic 20:12 ET CRITIC 08** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_08.md`): one UPHELD on the ANSWER 07 hashes (`B88302EF…` / `01DC50B1…` / `1DDD3CCE…`); clause (2) names first scoring look k=2, then still calls α_14 the "Fourteenth"; bar not edited; not scored; not bound. **CoS 20:15 ET assigned operator** to answer that one. **Operator 20:28 ET ANSWER 08** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_08.md`): one SUSTAINED; clause (2) labels α_14 the schedule's 14th term (the 13th scoring look); "Fourteenth" struck as a name for α_14 as a scoring look; FWER 4.6% labeled as 14 schedule terms including spent unused α_1; condition 1 unmet on the amended bytes; not scored; not bound. **CoS 20:30 ET assigned soften-critic** to attack those new hashes. **Soften Critic 20:48 ET CRITIC 09** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_09.md`): zero UPHELD on the ANSWER 08 hashes (`6CACBB0F…` / `1C5D9FFF…` / `1DDD3CCE…`); CRITIC 08's demanded α_14 term/scoring-look label is on these hashes; bar not edited; not scored; not bound. **CoS 20:45 ET assigned operator** to record that completed attack. **Operator 21:00 ET ANSWER 09** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_09.md`): zero objections recorded; none to sustain or overrule; condition 1 unmet on the link-amendment bytes; not scored; not bound. **CoS 21:01 ET assigned soften-critic** to attack those new hashes. **Soften Critic 21:11 ET CRITIC 10** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_10.md`): zero UPHELD on the ANSWER 09 hashes (`DC46EFA6…` / `B6DDDB6B…` / `1DDD3CCE…`); ANSWER 09 is a link-amendment only; CRITIC 08's demanded α_14 term/scoring-look label remains on these hashes; bar not edited; not scored; not bound. **CoS 21:15 ET assigned operator** to record that completed attack. **CoS 21:22 ET closed that assign** — two consecutive zero-UPHELD attacks; ANSWER 09 did not amend the bar; ANSWER 10 is not owed; Status=idle. Do not assign Soften Critic on a zero-objection record that did not amend the bar. **Do not run Soften Critic and Operator in the same fire.** CoS 21:22 ET `last_cos` handled `A_worker_done, B_new_judicial` at `5f09047`. **CoS 21:30 ET bookkeeping** — leave-off still said `next=operator`, which rang `E_idle_unassigned` on an idle desk; that line is now `next=idle`. `last_cos` handled `E_idle_unassigned` at `4244d97`. The 15m hub learning card names `R-SKIP-2TO1-FAVORITE` on trial. Founder saved **15m worker tick** (`golf-offshoot/docs/WORKER_AUTOMATION.md`, cron `7,22,37,52`). CoS prompt stop-rule is in `golf-offshoot/docs/COS_AUTOMATION.md`. A tick fires on a new official settle, a new paper fill, a pending window clearing, or a stale board. CoS starts when `crew_tick.needed` is true. Do not schedule CoS at 90s. Founder saved **15m worker tick** (`golf-offshoot/docs/WORKER_AUTOMATION.md`, cron `7,22,37,52`). A tick fires on a new official settle, a new paper fill, a pending window clearing, or a stale board. CoS starts when `crew_tick.needed` is true. Do not schedule CoS at 90s.
   - `lane-15m` — confirm the watch is healthy, do not double-start hubs, stay `SETTLE_PENDING` where there is no Kalshi `result`
   - `digest-figures` — generate the SOURCE figures half (`python -m golf_offshoot digest-15m`). Never write the caveats file
   - `digestor` — human caveats turn only, and **owed only on its five exceptions** (7f). A figures-only SOURCE refresh leaves this owed. A new caveat is the only write that clears it
   - `operator` — park/Soften only what the spine supports; put leave-off + desk on committed truth
   - `systems` — `manifest.json` merge; never drop the published `paper_win`, never invent a pending, never sum the lineages
   - `validator` — `python docs/observability-hub/validate_hub.py --strict --write-report` on the bytes about to publish; the hash-stamped report is its proof
   - `critic-invariants` — clerical, self-serving. Runs the nine method checks and writes `LEARNING_LANE_15M_CRITIC_FINDINGS.json`. It objects to nothing
   - `soften-critic` — the written attack. **Never auto-served**, and never in the same session as whoever authored the thing it attacks
   - **publish** — gym `PUBLISH_ARMED` may push the hub allowlist to `origin/master` from a master worktree when `material_publish_reasons` is non-empty. Do not publish a 90s heartbeat. Do not leave a corrected falsehood unpublished. Never sibling-HEAD-to-master. Cloud CoS/worker do not publish. Factory merge of #178 is still a PR merge
2. **Owed right now:** CoS 21:31 ET closed Lab invent (`decide_cos_action` closeout `worker_done`). Desk `Status=idle` Active role=chief-of-staff Job=—. Lab 21:30 ET invented **`CLOCK-HOUR-WRAP`** as `F-CLOCK-HOUR-WRAP-0-45` (`execution` false; skip `:00`/`:45`; rate 0.5). Gym 8765 observes origin farm via hub `maybe_fetch_origin_farm` (no Founder pull; cache gitignored; does not overlay executing `RULES.json`). Lab 20:42 ET already invented **`CLOCK-HOUR-OPEN`**. Lab 20:10 ET already invented **`CLOCK-HOUR-SECOND-HALF`**. Lab 19:36 ET already invented **`CLOCK-HOUR-FIRST-HALF`**. Lab 19:08 ET already invented **`CLOCK-QUARTER-BOUNDARIES`**. Lab 18:46 ET already dated `F-CLOCK-CLOSE-MINUTE-{15,30,45}`. `unused_legal_kinds` empty. `farm_hunger` still true (invent next kind or write `farm_menu_exhausted`) — I is not stamped; next CoS fire assigns Lab. Civil still blocked while hour-close executes. J/F/H not owed. Soften Critic of the promote path remains a **later session** from the farm author. Do not steal the chair. Do not score coinflip. Do not ADMIT. Do not arm. Consult **off**. Trading **NOT ARMED**. This PC must not sleep. next=idle
3. **Never collapse the two residual states.** A window pending for want of a Kalshi `result` is not the same as `KXBTC15M-26SEP071500-00`, which has an official `result=yes` and no book on this tree. Do not invent pnl for either. Do not merge lineage A and lineage B.
4. **Illustrator:** the wake owes a re-render when the PNG lags more than one window. Re-render from current files before a material publish. Never invent a chart. Do not put golf WC1 / Ill on this lane.
5. **`lab` PROPOSED 01 is RUN-ONLY**, not parked. Hurdle lives only in `golf-offshoot/docs/LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_01.md`. **`lab` PROPOSED 02 is RUN-ONLY**, then L1 **PARK** on its falsifier: `R-SKIP-2TO1-FAVORITE` in `LEARNING_LANE_15M_RULES.json` (`declared_at` 2026-09-08T16:53:00-04:00, `favorite_odds=2`, `execution=true` until Operator drops it, L1 scored). Artifacts: `golf-offshoot/docs/LEARNING_LANE_15M_LAB_PROPOSED_02.md` + `LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_02.md`. **`lab` PROPOSED 03 is RUN-ONLY**, not admitted: `R-SKIP-HOUR-CLOSE` (`declared_at` 2026-09-10T13:25:00-04:00, `skip_close_minute=0`, `execution=true`, `trials_to_date=2`). Artifacts: `golf-offshoot/docs/LEARNING_LANE_15M_LAB_PROPOSED_03.md` + `LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_03.md`. Not an ADMIT. Not a dashboard figure. Lab never self-admits. The clerical runner is armed by local `latest/RUNNER_ARMED` (gitignored). **Whitelist (trust-boundary move, not an append):** `illustrator`, `systems`, `digest-figures`, `validator`, `critic-invariants`. Human `digestor` is now in `JUDICIAL_NEVER`. Do not add `operator`, `lab`, or `soften-critic`. Do not put the figures generator in the `digestor` slot. Gym `PUBLISH_ARMED` may push the hub allowlist; #178 factory merge is still a PR merge.
6. **`KXBTC15M-26SEP072245` does not exist** (22:25–22:50 EDT `--once` argparse). **Eight more windows `100315` through `100500` (03:00–05:00 EDT 2026-09-10) also do not exist.** Detector `KXBTC15M-26SEP100515-15`. Do not backfill either hole. Locked lineage-A is not an unbroken run.
7. **Evidence bar IS binding** (ANSWER 20, 2026-09-10). Bound hashes MD `6490a3307a1acb73897acfa2bb8c4439e834d7ba52266bb96aa2ad51005503e8` / JSON `1f36a807bb789126ee66f82ad466d5bf841b3fe8296ce6358de18d8005d9fba6`. Binding is for **Admissible** scoring. Established remains unreachable (`favorite_odds=2` not verifiably pre-registered). `R-SKIP-2TO1-FAVORITE` L1 scored then **PARK** on the registry falsifier (`n=70`, `skip_count=1`, clauses 1/4/5 fail; class `crew`). Scorecard lives only in Operator artifacts — never hub, digest, `manifest.json`, or `records[]`. `R-SKIP-COINFLIP` unscored. Consult **off**. Honer dark. Lab PROPOSED 03 is **RUN-ONLY** (`R-SKIP-HOUR-CLOSE`, `execution=true`). Favorite off the selecting seat. HOLD stands. Trading **NOT ARMED**. next=idle. **Systems 2026-09-10 09:10 EDT bind-candidate (then CRITIC 19 / ANSWER 19 / CRITIC 20 / ANSWER 20 bind):** Founder browser fee pin `founder_browser_bytes` (SHA-256 `c326a69f596a11e8f8be2620402d39a8d4823920c21cc97c93a114d862699601`, file `golf-offshoot/docs/kalshi-fee-schedule.pdf`); half-spread profile n=624; `check_delta_above_detection_floor` α-key; paper mid/last strings; WATCHED PROPOSED 02; `declare_rule` informing-mark gate; consult fail-closed (flag stays false); hub-down/gap ntfy; hub client-abort swallow; automations docs on `cursor/honer-15m-sibling`; `hub_autostart_registered`. Leftover "unpinned fee hash" face struck. **Do not GET the PDF every 90s.** Gym 12h 429 is drift detection, not the pin clock. CRITIC 18 closed ANSWER 17 hashes `D9FDA991…` / `F7E8F681…`; those bytes were not the bind-candidate; ANSWER 18 was not written. Systems 2026-09-09 19:51 EDT landed the gym PaperWatch fee pin (12h PDF probe; 429 off the bar; `series_fee_regime_matches`). `schedule_sha256` still empty. Systems 2026-09-09 16:50 EDT named honer as the discovery organ, landed a dark AND-skip compositor, and retried the fee-schedule probe (HTTP 429 at 17:01:17; sha256 empty). Systems 2026-09-09 17:40 EDT landed honer-only search starvation (catalog/rules hashes changed on honer files; factory rules untouched). **Soften Critic 18:28 ET CRITIC 11** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_11.md`): six UPHELD on the 16:50 destination/compositor/fee-retry factory bar hashes (`9C323D59…` / `645A82F8…`); bar not edited; honer catalog/rules starvation hashes remain; consult off; not scored; not bound. **Operator 18:42 ET ANSWER 11** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_11.md`): six SUSTAINED; destination paragraph names both resolved snapshot paths, both compositor families (`H-SKIP-WIDE-SPREAD` is OR(spread ≥ δ, posted_yes ≥ θ)), honer-skip keeps factory `rule_id` and overwrites reason, protocol exam contrast is gross `pnl_exam − pnl_fill_all` and is not this bar, Founder-named is not a machine gate; owed fee row restated to 17:01:17. **Soften Critic 19:10 ET CRITIC 12** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_12.md`): three UPHELD on the ANSWER 11 hashes (`A0115B38…` / `6074217E…`). **Operator ANSWER 12** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_12.md`): three SUSTAINED; `consult: honer_and_skip` is in-memory only; writing `consult_enabled: true` leaves the dark path and a flag-only snapshot raises through unguarded `compose_and_skip`; missing/null δ is `0.0` so `spread ≥ δ` holds for every non-negative quoted spread under `H-SKIP-WIDE-SPREAD`. **Soften Critic 19:26 ET CRITIC 13** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_13.md`): zero UPHELD on the ANSWER 12 hashes (`53AC9D6A…` / `BA7D40DA…`); CoS closed — ANSWER 13 would record nothing. The gym-pin amend is **new factory-bar bytes**. **Soften Critic 20:52 ET CRITIC 14** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_14.md`): three UPHELD on those hashes (`C9D3FF62…` / `17A3788C…`); `snapshot_absent` is `state: PASS` and will not block a suite green; labeled `latest/` is the fallback shape; ratchet paragraph still says seven. Bar not edited. **Operator 20:58 ET ANSWER 14** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_14.md`): three SUSTAINED; face now names `snapshot_absent` as suite PASS, both resolved probe/snapshot paths, and eight method checks. Bar amended. Honer catalog/rules starvation hashes remain. **CoS 21:00 ET assigned soften-critic** on those new hashes (`72A4A4EE…` / `999E33E4…`). **Soften Critic 21:12 ET CRITIC 15** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_15.md`): one UPHELD on those hashes; eighth-check detail still says a "half-pass" is named on the bar; these hashes name `state: PASS` / suite green and do not contain "half-pass". Bar not edited. Honer catalog/rules remain. **Operator 21:28 ET ANSWER 15** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_15.md`): one SUSTAINED; leftover citation named on the face (eighth-check detail and docstring still say "half-pass" / "silent half-pass" "named on the bar"; ANSWER 14 hashes did not contain that word). Bar amended. Honer catalog/rules starvation hashes remain. **CoS 21:30 ET assigned soften-critic** on those new hashes (`3C8369B4…` / `1F183CE1…`). **Soften Critic 21:42 ET CRITIC 16** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_16.md`): zero UPHELD on those hashes; CRITIC 15's demanded leftover half-pass citation is on the face; bar not edited. Honer catalog/rules remain. **CoS 21:45 ET closed that assign** — ANSWER 16 would record nothing. **Operator 21:50 ET** dropped `founder_read_once` from bind (Founder GO). Ninth method check `bind_has_no_founder_read_once`. Honer keep no longer waits on that stamp. **CoS 21:50 ET assigned soften-critic** on the new hashes (`2ACB998B…` / `B1044CCE…`). Protocol: `golf-offshoot/docs/HONER_15M_PROMOTION.md`. Consult is **off**. **Soften Critic 22:20 ET CRITIC 17** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_17.md`): one UPHELD on the drop-read-once factory hashes (`1D29ABCD…` / `A5FCFAB7…`; desk prefixes did not match these files); MD `:238` still says condition 3 is unmet after these hashes dropped that bind condition; JSON `unreachable_because` already struck the phrase. Bar not edited. Honer catalog/rules remain. **CoS 22:32 ET assigned operator** to answer that one. **Operator 22:50 ET ANSWER 17** (`LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_17.md`): one SUSTAINED; MD `:238` leftover "condition 3 is unmet" struck so the Established face agrees with JSON `:124`; bind stays two conditions; bar amended; honer catalog/rules remain; consult off; not scored; not bound. **CoS 23:04 ET assigned soften-critic** on those new hashes (`D9FDA991…` / `F7E8F681…`). **Soften Critic 23:12 ET CRITIC 18** (`LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_18.md`): zero UPHELD on those hashes; CRITIC 17's demanded MD `:238` leftover "condition 3 is unmet" strike is on the face; JSON `:124` already lacked the phrase; bind stays two. Bar not edited. Honer catalog/rules remain. **CoS 23:20 ET closed that assign** — ANSWER 18 would record nothing; Status=idle; did not assign Operator or Soften Critic on those hashes. Protocol: `golf-offshoot/docs/HONER_15M_PROMOTION.md`. Consult is **off**. Founder 2026-09-09 dropped condition 3. **CoS 2026-09-10 09:10 ET assigned soften-critic** on the Systems bind-candidate (`D25D0227…` / `4D9E86C2…`). That assign completed: CRITIC 19 two UPHELD / ANSWER 19; CRITIC 20 zero UPHELD; ANSWER 20 `binding: true`; favorite L1 PARK. next=idle
   - **Do not score `R-SKIP-COINFLIP`.** Eligible settled after `declared_at` is **172** as of 2026-09-10 07:35 ET (see `LEARNING_LANE_15M_OPERATOR_NOTE_2026-09-10_GAP_AND_N.md`). `execution` stays false. Report the eligible count only; do not open a post-declaration outcome file.
   - **The coinflip band is not verifiably pre-registered.** Same 8h 07m finding as CRITIC 01. L1 still cannot Establish that rule. **`favorite_odds=2` is also not verifiably pre-registered** (ANSWER 03 row: 2/24 published marks ≥ 2/3; 19h 06m gap). L1 cannot Establish `R-SKIP-2TO1-FAVORITE` either.
   - **Why it is binding, and why Established is still unreachable:** Founder 2026-09-09 dropped condition 3 (`founder_read_once`); bind is conditions 1 and 2. Both met on ANSWER 20: CRITIC 20 zero UPHELD on ANSWER 19 hashes (`7C1C1CB3…` / `7A934A4E…`); Operator recorded the completed attack and set `binding: true`; `write_critic_findings()` after that face; findings `passed: true`. CRITIC 18 zero UPHELD on ANSWER 17 hashes (`D9FDA991…` / `F7E8F681…`); those bytes were not proposed to bind; ANSWER 18 was not written. Fee pin is `founder_browser_bytes`, not a gym HTTP 200. Do not paper-bind-anyway. Do not unbind. Favorite L1 is PARK, not an ADMIT. next=idle
   - **`decide()` is live in the running hub** (`paper.py` → `consult_registry` → `rules.decide()` → dark `compose_and_skip`). `R-SKIP-HOUR-CLOSE` is the paper executing selection rule (`execution=true`) after Operator RUN-ONLY 2026-09-10 13:36. `R-SKIP-2TO1-FAVORITE` L1 PARK stands and that dead row is off the selecting seat. Honer consult is **off**. `currently_reachable` stays **false** because the parked favorite is not verifiably pre-registered (L1 cannot Establish it) and `R-SKIP-COINFLIP` still cannot Establish. The bar being binding does not make Established reachable. Do not flip `currently_reachable` after an L2 window has closed.
7b. **Part A landed and is now actually running.** `digest-figures` proves SOURCE. Human `digestor` is keyed on `LEARNING_LANE_15M_SOURCE_DIGEST_CAVEATS.md` only. `validator` is on the clerical whitelist with `validator_report.json` as proof. Proven in production, runner pass **10:46:45**: `served=[illustrator, systems, digest-figures, validator]`, `failed=[]`, with `operator` and `digestor` held for human. Gym `PUBLISH_ARMED` is the material Pages hook (hub allowlist only).

7c. **Part 0 — why Part A sat inert for ninety minutes.** Two independent defects, both needed for silence:
   - `read_git_tip` resolved refs only inside the **worktree** git dir. A linked worktree keeps branch refs in the common dir named by `commondir`, so every branch SHA read as empty and `git_tip` changed **only when the branch name changed**. A commit on the current branch looked like no change.
   - `hub_code_files` watched `operator_surface/*.py` only. The entire `learning_lane_15m` package — which PaperWatch and the clerical runner execute **inside** the hub — was unwatched.
   Both fixed in `operator_surface/reload.py`. The supervisor re-exec'd its own child; nothing was killed and no second hub was started. `process_matches_disk` is the standing check if this regresses.

7d. **Part 1 — the invariant suite.** `learning_lane_15m/invariants.py`, artifact `latest/invariants.json`, printed on every `learn-15m`. Four checks: `digest_matches_ledger`, `process_matches_disk`, `watch_is_collecting`, `clerical_roles_clear`. It runs **after** the runner pass, so a digest the runner just regenerated is not reported stale. Adding a check is Founder-free; **removing or weakening one is not.**

7e. **Also fixed:** `tests/test_learning_digest.py` called `write_digest()` with no root, so every full test run silently rewrote the **published** SOURCE digest. That is how the digest appeared to refresh at 10:40 before `digest-figures` had ever served. The generator now only touches the real spine through the runner.

7f. **Part 2 — human `digestor` has triggers again.** Five enumerated exceptions in `learning_lane_15m/triggers.py`, the cases where generated figures cannot express what changed: `paper_join_missing_grew`, `book_open_no_join`, `window_sequence_gap` (the `072245` shape), `settle_contradicts_book`, `unrecorded_cost`. The every-settle trigger is **not** restored. A detector that cannot read its evidence raises to Operator rather than staying silent — a detector that cannot see is not a detector that saw nothing.

7g. **Part 3 — three honesty boxes are derived, one needs evidence.** `learning_lane_15m/honesty.py`. Lineage, missing-join and hub-tree come off files, and the **derived verdict wins**: prose can shut a box, never open one, and deleting a row from the desk does not open the gate either. The hub box counts hub **trees** off the process table — the criterion is preserved exactly as written, a supervisor plus its child is one hub. `No invented charts or pnl` stays judgment and now needs a PID, a file hash or a timestamp; `**PASS**` alone no longer opens it. The parser was **narrowed, not widened**.

7h. **Part 4 — Operator no longer over-fires or false-clears.** A routine settle names `digest-figures`, `systems`, `validator` and nobody judicial. Operator is named by six enumerated exceptions (`park_aged`, `settle_contradicts_book`, `paper_join_missing_grew`, `window_sequence_gap`, `falsifier_fired`, `rule_reached_n`, `lab_proposed`). `operator_write_addresses_owed` is shaped like `material_publish_reasons`: a park write clears Operator only when the new text names what Operator was owed for, and **if it is unclear Operator stays owed**. No exception class was dropped to shorten the list and no Operator verdict is automated.

7i. **Part 5 — the Critic has a body.** Split like Digestor. `critic-invariants` is a deterministic checker, **on `CLERICAL_WHITELIST`**, proof artifact `golf-offshoot/docs/LEARNING_LANE_15M_CRITIC_FINDINGS.json`. `soften-critic` stays in `JUDICIAL_NEVER` — a written attack is not hash-provable. The missing trigger is now a **repo-side event class**, `artifact_unreviewed`: a watched artifact's hash changed and no Critic finding exists for that hash. Watched: the evidence bar (both files), the rule registry, the Lab PROPOSED, and the honesty-gate stamp section of the desk. Findings are keyed by content hash, so editing a bar re-owes the Critic on the new text and cannot be cleared by editing something else. Proven live 11:11:57: `served=[critic-invariants]` while `soften-critic` stayed owed.

7j. **Part 6 — the ratchet.** Eleven printed method lines in `critic.py` in `CHECKS`: `matched_exposure_control`, `delta_above_detection_floor`, `holdout_is_forward_only`, `fee_adjusted_book_is_binding`, `declared_at_precedes_scored_windows`, `trials_counter_is_consistent`, `fee_schedule_hash_recorded`, `series_fee_regime_matches`, `bind_has_no_founder_read_once`, `half_spread_profile_recorded`, `hub_autostart_registered`, plus desk-only `honesty_stamp_is_fresh`. A checker cannot find an unknown failure mode — that is why every flaw analysis finds becomes a permanent check. **Adding one is Founder-free; removing or weakening one is not.**

8. **Soften Critic is hired and has now run.** Skill `.cursor/skills/gpf-soften-critic/SKILL.md`. Not on the clerical whitelist and never auto-served. Part 7 attack: `LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_01.md`, written in a session separate from the one that drafted the bar (#174). Operator's answers are a **separate turn** from the objections — one turn may not both object and dismiss. Both turns did real adversarial work in both directions:
   - The Critic **rejected three of the seven briefed premises as factually wrong about the drafted text** — "thirteen eligible windows" is nowhere in a bar that says n=40 four times; L2 *is* forward-only and enforced in `rules.py`; and multiplicity was not uncorrected. It upheld sharper versions of each instead.
   - Operator **overruled the Critic's sharpest new claim.** The Critic argued the omitted bid/ask spread is worst at 50/50 *unlike* the fee. Operator recomputed: `s/(p+s)` is monotone decreasing in p, the same direction as the fee. The rule avoids neither cost's worst region — which is a worse result for the rule than the Critic's version, and it is the correct one.
   - **The ratchet closed on its own author.** The Critic found three defects in code committed an hour earlier, and Operator then found that the fix for one of them was only half applied: `_critic_token` reached the human artifact-proof path but `serve_role` still compared raw fingerprints, so the runner kept clearing the Critic on a heartbeat. Fixed at the same place `systems` gets its material test, with a guard against the opposite failure — a run that reviews a newly-changed artifact *has* done work, and treating it as a heartbeat would deadlock the role once `artifact_unreviewed` stopped firing.
8b. **Hub autostart task is registered on this gym PC.** Task `GatedFormalization-15mLearningHub` exists (`Ready` as of 2026-09-10 09:09 ET). `WakeToRun=false` still does not survive sleep — Founder confirms this PC stays awake. Cloud CoS/worker automations are treated as on `cursor/honer-15m-sibling` unless a fire proves otherwise (wrong branch writes nothing). Scripts remain on `master` (`b8b4d12`). Do not re-register. Do not start a second hub.
8c. **Part 8 — one of five landed, and the other four have reasons, not excuses.**
   - **Landed: the judicial-silence counter.** Every owed line carries `ticks_unanswered` and the tick prints `silent N ticks`. Silence is now a number.
   - **Landed: gym material Pages publish.** `PUBLISH_ARMED` (gitignored, this gym tree) may push the hub allowlist to `origin/master` from a master worktree when `material_publish_reasons` is non-empty, `--strict` is clean, `watch_is_collecting` PASS, and lineage A `ledger.json` is present. The clerical runner still does not commit or push. Cloud CoS/worker do not publish. Never sibling-HEAD-to-master. Factory merge of #178 is still a PR merge.
   - **Partly landed: `score_rule` exists and `decide()` is in the paper path.** `score_rule` **ran** L1 of `R-SKIP-2TO1-FAVORITE` (Admissible, PARK on falsifier). `R-SKIP-COINFLIP` remains unscored, `execution` false. Do not score it. Do not score in a proposing, attacking, or amending turn.
   - **Partly landed: a runtime for Operator.** Part 4 gave Operator six enumerated triggers and a materiality test on its clear. The bar is now binding; Operator used it for one Admissible score. ADMIT authority is still unused — PARK is not an ADMIT, and no ADMIT is proposed.
   - **Not landed: promoting `learning_lane_15m` into the gated method.** The bar is binding for Admissible scoring. Established is still unreachable on this tree's skip-on-mark classes. HOLD lift is Founder GO only and is not this plan. Honer stays dark. Lab PROPOSED 03 (`R-SKIP-HOUR-CLOSE`) is **RUN-ONLY** (paper only; `execution=true`). Not scored. Not an ADMIT.
9. **Founder HOLD 2026-09-07 stands: no series other than `KXBTC15M` until this loop is honest.** Only Founder lifts it — not Operator, not CoS, not a later bot reading a tidy tick. Lifting it opens a **new lane**; it does not add a ticker here. Procedure: `golf-offshoot/docs/LEARNING_LANE_EXPANSION.md`. Everything else parked (weekly honesty rollup, expanding the series, CFB websocket observe-only) is in `golf-offshoot/docs/LEARNING_LANE_15M_METHOD_PARK.md` with the explicit trigger that would reopen it.
10. **Golf idle stays ON** (WC3+ only on a new settled week, on a fresh Founder GO that names the next invent). A CoS-assigned `KXBTC15M` 15m paper PROPOSED is **not** golf idle-breach. Nothing on the 15m lane clears golf idle, retunes golf θ, or rewrites `golf-offshoot/docs/phase1_dryrun/OPERATOR_STATUS_STAMP.md`.

## Hard NOs

- No Kalshi API keys, cash scopes, orders, or private endpoints
- AI never deposit / withdraw / transfer
- Do not retune golf θ from 15-min
- Do not invent win/lose or use DIY CFB averages as official settle. Display prices (`yes_bid` / `yes_ask` / `last_price`) are not settle evidence
- Do not put golf WC1 / Ill under `learning_lane_15m`
- Charts render from real files only. A chart no real file can drive stays `not yet available` — never invented, never back-filled
- Do not merge, sum, net or average the two paper lineages, and do not drop lineage B's published `paper_win` `+1.67` to make one clean story
- Do not Soften the SOURCE CONFLICT away. Either one readable lineage story or an explicitly labeled dual lineage — a silent merge fails the honesty gate
- Paper fills are not ADMITs (`lab_admits=false`). No edge established / banked edge / skill-met / productize on this lane
- Do not glob a second series into this lane’s ledger, bar, or globs. Expansion scaffold: `golf-offshoot/docs/LEARNING_LANE_EXPANSION.md`
- Soften Critic **is hired** — do not skip it, do not let it share the authoring session, do not put it on the clerical whitelist
- Do not add Founder read-once (or any Founder acknowledgement) as a condition of the 15m bar becoming binding. Bind is Critic+Operator and critic-invariants. Founder remains for **arm**, cash, Kalshi keys, HOLD lift, and golf C2/C4/WC3+. Replacement of the executing registry row after consult has lived is crew, not Founder.
- Do not merge leftover oil-hunt / `cursor/eia-window-job2` dirt into this track

## Resume commands

```powershell
cd golf-offshoot
$env:PYTHONPATH = "src"
python -m golf_offshoot learn-15m                        # learning tick — what is owed and why (start here)
python -m golf_offshoot shell --lane learning_lane_15m   # PaperWatch starts itself (~90s cycle) and runs one clerical pass each tick
# Windows: golf-offshoot/scripts/windows/Open-15m-Learning-Hub.bat
python -m golf_offshoot lane-15m --watch                 # CLI-only repeat, same cadence (do not also run learn-15m-runner)
python -m golf_offshoot learn-15m-runner --kill-runner   # write latest/RUNNER_KILL; does not touch PaperWatch
python docs/observability-hub/validate_hub.py --strict   # from repo root
```

One hub process only. A hub may already be live on `127.0.0.1:8765` — check before starting another, and never kill a running one to start your own.

Public viewer: `docs/observability-hub/` (read-only; no controls).
