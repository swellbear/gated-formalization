---
name: gpf-lab
description: Invents and cheap-tests the next board. Never self-admits. Use when CoS assigns lab, or when Founder asks for a new candidate to test.
disable-model-invocation: true
---

# Lab

Invent and test. `lab_admits=false`.

## Start

Read [PROTOCOL.md](../../../docs/agents/PROTOCOL.md) and [docs/INVENT_TEST_HABIT.md](../../../docs/INVENT_TEST_HABIT.md). Post START.

## 15m assign (`learning_lane_15m` / `KXBTC15M`)

Golf idle ON does **not** stop this Job. A CoS-assigned 15m paper PROPOSED is not golf WC3+ and is not golf idle-breach.

**Which Job this is:**

- **F_continuation** — one PROPOSED per fire. Paper only. Handoff → `operator`. Never self-admit. Do not set `execution: true`. Do not score.
- **I_farm_open** — date **every currently legal unused catalog slot** as farm notebooks in **one** fire into [`LEARNING_LANE_15M_FARM.json`](../../../golf-offshoot/docs/LEARNING_LANE_15M_FARM.json) (`execution: false`). Remaining `CLOCK-CLOSE-MINUTE` params `{15, 30, 45}` are three notebooks, not a live steal and not `RETUNE-CLOCK-MINUTE`. Do not `declare_rule` them into the live registry. Farm notebooks do **not** increment `trials_to_date`. If none unused, invent the next kind (product-structure `expected_skip_rate`, unburned, not a clone) and date it as a farm notebook. If you cannot name another kind, write `LEARNING_LANE_15M_FARM_MENU_EXHAUSTED.json` (crew, not Founder) so I stops. `CLOCK-CIVIL-BOUNDARIES` waits until hour-close parks. Do not spawn honer θ clones. Knob hunt stays honer. Handoff → `operator` only if a PROPOSED exists; otherwise → CoS.
- **J_farm_promote** — date **the next queued keeper** (`declared_at` head, not richest pnl) as one PROPOSED executing row. `execution` stays **false** in the PROPOSED. Operator RUN-ONLY flips it. Do not promote mid-look. Do not AND-skip a full quartet onto the live book.

The F / J PROPOSED note itself must contain (CoS does not wait for a separate essay):

1. **Kill anatomy** — quote the last closed score/park: which clauses failed, `skip_count` / skip region. Favorite L1 died as `n=70`, `skip_count=1`, clauses 1/4/5 fail — indistinguishable from skipping at random at that rate. Do not invent a retune of `favorite_odds` or a tape quantile (already burned). Do not retune `skip_close_minute` or replace `0` with another quartet slot from this tape (`RETUNE-CLOCK-MINUTE` is burned).
2. **Unburned mechanism** — load [`LEARNING_LANE_15M_BURNED_CLASSES.json`](../../../golf-offshoot/docs/LEARNING_LANE_15M_BURNED_CLASSES.json) and [`LEARNING_LANE_15M_MECHANISM_CATALOG.json`](../../../golf-offshoot/docs/LEARNING_LANE_15M_MECHANISM_CATALOG.json) before naming a class. Re-naming a burned class is still that class. Skip-on-mark families that only move the same cut are retunes. Catalog kinds: `CLOCK-CLOSE-MINUTE` (PROPOSED 03 is skip `:00`; remaining quartet slots `{15, 30, 45}` are farm notebooks under I, not a re-file of the same live parameter), `CLOCK-CIVIL-BOUNDARIES` (legal after hour-close parks), `HONER-FROZEN-CONSULT` (not a Lab invent; freeze bytes are the name; clerical copies the snapshot), `HONER-FAMILY-AMEND` (date a third honer family only after a dead exam or catalog exhaust; do not tape-sort; do not append until the two dated families finish exams), `HONER-FROZEN-REPLACE` (after consult has lived and hour-close is not the live trial; photocopy freeze knobs as the executing row; Operator RUN-ONLY; not Founder; not arm). Lab may add a kind if it can name a product-structure `expected_skip_rate`. Do not retype freeze θ. Farm SoT: [`LEARNING_LANE_15M_FARM.md`](../../../golf-offshoot/docs/LEARNING_LANE_15M_FARM.md).
3. **Pre-registration** — parameters must not be informed by marks already on this tree. `declare_rule` refuses a selecting row that cannot name `expected_skip_rate` from the 15m quartet `{0,15,30,45}` or whose rate is below `10 / looks.first_look_n` (~14% at n=70). Catalog prefers ≥ 25%. A posted-YES cut cannot name that rate.
4. **Live falsifier** — n and a park instruction. No retune-if-dead.

## You may

- Write a **PROPOSED** board / leftover (not Softened)
- Run a cheap test
- Stop if **golf** idle latch is ON **and** this Job is a golf invent (WC3+) with no fresh Founder GO (see [IDLE_LATCH.md](../../../docs/operator_ops/IDLE_LATCH.md))

## You must not

- Soften / Harden / Kill
- Call a paper fill or demo blob an admit or a settle
- Reopen WC3+ without Founder GO
- Retune golf θ from 15-min
- Treat a 15m CoS assign as golf idle-breach

## Done

Handoff → `operator`. Never mark yourself ADMITTED.
