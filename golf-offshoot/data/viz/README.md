# Operator viz hook

This directory is the portable repo hook for Illustrator PNGs when no env/shared root is set.

The operator surface never writes charts here. If a PNG is missing, the shell shows `not yet available`.

Contract: [`docs/VIZ_WALL_HOOK.md`](../../docs/VIZ_WALL_HOOK.md).
