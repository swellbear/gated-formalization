# Lab — golf Farm/Honer namer: REFUSED

**State:** **REFUSED.** Not PROPOSED. **Not** Softened, **not** admitted, **not** a notebook, **not** a dashboard figure. `notebooks` stay `[]`. `execution=false`. Lab never self-admits.
**Lane:** `golf_kalshi` only. Not `learning_lane_15m`. Not `KXBTC15M`. Not `HONER-FAMILY-AMEND`.
**Role:** `lab` — `lab_admits=false`.
**Admit?** N · **Soften?** N · **Trading ARMED?** N · **Keys / orders / cash?** none
**Written:** 2026-09-12 16:59 EDT
**Handoff:** `chief-of-staff` (no PROPOSED, so not Operator)

**Gate this was written under.** Founder M3SS YES: look at the golf paper tickets and either date a real golf family or refuse. After [#194](https://github.com/swellbear/gated-formalization/pull/194) Farm/Honer can see the paper book; empty notebooks until dated. Naming is Lab. Farm/Honer must not auto-name. Factory overlay must exist per opened market: the namer sat. An empty catalog after that look is honest. Filling the pile with a fake family is not.

**What this is not.** Not a WC3+ weekly invent. Golf idle / WC1 stays the museum of the old weekly claim (`phase1_dryrun/`). Not a 15m Lab fire (`F_continuation` / `I_farm_open` / `J_farm_promote`). Not `HONER-FAMILY-AMEND`. Not a copy of `H-SKIP-RICH-YES` / `H-SKIP-WIDE-SPREAD` / `H-SKIP-THIN-BOOK` onto golf. Does not put golf on the 15m quote bus. Does not retune golf θ from 15-min. Does not set `execution=true`. Does not arm.

---

## Book Lab looked at

Live gym tape `golf-offshoot/data/golf_kalshi/paper/ledger.json` + `latest/last_tick.json` (2026-09-12 16:55 ET tick). Factory `decide_golf` already overlayed every opened market this tick (`markets=911`, `decisions=916`, `fills=0`, `skips=911`).

| Field | Count |
|---|---|
| Tickets | 17 |
| Open (`SETTLE_PENDING`) | 7 |
| `paper_lose` (Kalshi `result=no`) | 6 |
| `paper_win` | 0 |
| `paper_exit` (path, not a Brier row) | 4 |
| `lived` | 0 / 17 |
| `lived_fills_settled` | 0 |
| `trading_armed` | false |

Events: Irish Open top-10 `KXDPWTTOP10-AMIO26` (10), Irish Open winner `KXDPWORLDTOUR-AMIO26` (4), 2027 Europe Ryder captain `KXRYDERCUPCAPTAIN-2027EUR` (3). Sleeves: week 14, slow 3, fast 0. Field: `kalshi_listed` 13, `espn` 4.

This tick's skip mix (factory, already named): `no_model_p` 676, `no_edge` 180, `no_field` 25, `no_quote` 23, `unmatched` 2, `mix_event_cap` 2, `mix_sleeve_full` 3. Worthy 5, picked 0/0/0, week overweight catch-up.

---

## Why Lab will not date a family

A golf Farm/Honer family has to be a **named live-skip overlay** on every opened golf market: product-structure `expected_skip_rate`, unburned, not informed by this tree's marks, with n and a park instruction. These tickets do not support that.

1. **No lived skip to name.** Every ticket is `lived=false`. Listed fills are already observation, not promote fuel (`GOLF_KALSHI.md`). A family dated off observation PnL would pretend a lesson the recipe already refused to promote.
2. **n cannot carry a falsifier.** Six Kalshi-settled loses, zero wins, four exits. 15m density floor is `10/n` at first look n=70. This book cannot name that rate from a golf product partition, and it cannot fire a live falsifier.
3. **Any ask/listed cut from this book is tape θ.** The six settled rows are cheap listed top-10 (`yes_ask` 0.01–0.15) that lost. Picking skip-listed, skip-penny, or skip-`ask < 0.05` from those marks is selecting on this tree. Pre-registration forbids it.
4. **Bitcoin families do not transfer.** `H-SKIP-RICH-YES` (posted YES ≥ θ), `H-SKIP-WIDE-SPREAD`, and `H-SKIP-THIN-BOOK` (missing bid/ask / tight spread) are 15m Honer classes. Most of these golf quotes have `yes_bid=null` / `spread=null`. Copying them onto golf is the thing this Job forbids. Missing-bid is not a golf-native named lesson just because the BTC organ uses it.
5. **Tour buckets are not a mechanism.** Tickets cluster on DP World / Irish Open because that is what the watch filled. Dating `G-SKIP-DP-WORLD` or "the family is Irish Open" is a catalog label, not a skip overlay on every opened market (PGA, LIV, majors, captains, …).
6. **Factory already overlays opened markets.** `decide_golf` skipped 911 of 911 this tick for named complete-gate reasons. The hole was that the **namer had never sat**. Lab sat. Empty notebooks after that look are the overlay being honest, not a missing skip class.

Refused names (do not date these from this book):

| Refused | Why |
|---|---|
| `H-SKIP-RICH-YES` / `H-SKIP-WIDE-SPREAD` / `H-SKIP-THIN-BOOK` on golf | Copying Bitcoin Honer families |
| Skip `kalshi_listed` / skip `yes_ask ≤ 0.01` / skip `yes_ask < 0.05` | Informed by this book's 1¢ listed losses |
| Skip slow / Ryder captain 2027 | Three pending tickets; no settle; not a named rate over all opened markets |
| Skip week-overweight / `no_model_p` / `no_edge` | Already factory `decide_golf` |
| WC3+ weekly claim | Golf idle ON; this Job is not that invent |
| 15m civil / clock / coinflip / favorite | Wrong lane |

---

## What stays on disk

- `data/golf_kalshi/latest/farm.json` `notebooks: []`, `idle: true`, `execution: false`. Organs keep copying existing notebooks. They do not invent.
- Golf Honer `consults_15m: false`, `consults_factory: false`.
- WC1 dated FAIL / park unproven. WC2 PARKED/REJECTED. Idle ON for WC3+.
- 15m chair, farm menu, and Honer family 3 are untouched by this note.

A later golf family is legal only when a **golf-native** product-structure skip can be named without this tree's marks, with a live falsifier, still `execution=false`. Palshi does not auto-date. Farm/Honer do not auto-name. Lab may refuse again.

Trading **NOT ARMED**.
