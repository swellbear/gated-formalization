# Soften Critic — attack on Systems 2026-09-09 19:51 gym-pin factory evidence-bar hashes (CRITIC 14)

**Role:** Soften Critic · **Date:** 2026-09-09 · **Lane:** `learning_lane_15m` · series `KXBTC15M` only
**Branch:** `cursor/honer-15m-sibling` at `eaa7f25` (CoS assign). Factory bar last touched at `c514015` (Systems gym fee pin). Git blobs of the two attacked bar files still equal `c514015` (`36230527…` / `b0901d8c…`).

**Session separation.** This session did not draft the bar, did not write CRITIC 01–13, did not write ANSWER 01–12, did not land `c514015` / `1b21a70` / `86e0cce`, did not declare `R-SKIP-2TO1-FAVORITE`, and did not RUN-ONLY it. `PROTOCOL.md` forbids one turn both objecting and dismissing.

**This is an attack, not a verdict.** Operator answers each objection in a turn that is not this one. Nothing here is a Soften, an ADMIT, a RUN-ONLY, a park, a score, or a proposal. No rule was scored. `R-SKIP-COINFLIP` was not revived and its band was not retuned. Consult was not enabled. `binding` stays `false` exactly as I found it. Trading is **NOT ARMED**.

**Why this file exists.** Desk Job 2026-09-09 20:30 ET: attack the Systems 2026-09-09 19:51 gym-pin factory evidence-bar bytes (`C9D3FF62…` / `17A3788C…`). Written objections only. Do not probe the fee PDF. Honer catalog/rules starvation hashes remain after this attack.

The gym-pin amend (`LEARNING_LANE_15M_EVIDENCE_BAR.md` / `.json` at `c514015`) named a 12h PaperWatch PDF probe, 429 off the bar, and `series_fee_regime_matches`. Those are not the ANSWER 12 hashes. Condition 1 is correctly `met: false` for that reason. This file attacks the **new** bytes.

---

## Artifacts attacked, by SHA-256

Computed independently via the same newline-normalised `read_text` → UTF-8 digest `critic.artifact_digest` uses (copied, not imported: this tree's package import pulls `pydantic`, which is not installed in this fire). Byte hash equals the normalised hash on every file below (0 CRLF pairs), except the honesty section, which is a slice of `DESK.md`.

| File | SHA-256 (bytes = normalised) | Last findings hash | CRITIC 13 attacked | Unreviewed? |
|---|---|---|---|---|
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR.md` | `C9D3FF62CA18FA3BE7F1BCD1F1764965B2886735D6467F9756DE5A1643EB7BB6` | `5F2AA5F5…` | `53AC9D6A…` | **Y** — Systems gym-pin amend |
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR.json` | `17A3788CA4889366ED7E3BE15B4C6A0D5D326E6922525A6A65B958C03F20558A` | `2611C255…` | `BA7D40DA…` | **Y** |
| `golf-offshoot/docs/LEARNING_LANE_15M_RULES.json` | `BBD87E52EE6FC1E5F797C676EA6038F4C77404DDE7F29E21128BD75BF4B802D0` | `CD25DD72…` | same digest | **Y** — unchanged since `3c89a7f`; still unreviewed by findings |
| `docs/agents/DESK.md` `## Honesty checklist` | `96BF9A39A916874DD8CF41DD74662A5DFDCBB898D3ABEA9DEC57C1A0C1330108` | `07055605…` | `25D1D5F2…` at `70d4786` | **Y** — restamp names gym-pin / this assign |
| `golf-offshoot/docs/LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_01.md` | `83D4463B4BB55D327485E3874A4BF3DAA54727D7ED37F56C124C6E8913DAF0B4` | `83D4463B…` | same | N — still the watched `lab_proposed` path |
| `golf-offshoot/docs/LEARNING_LANE_15M_FEE_SCHEDULE_PROBE.json` | `5DD35C25BADDF941803558D4D7DF871D4BA5CE5620588CE895DDFC7D0CCCC36C` | — | same | not `WATCHED`; cited by the restated 17:01:17 owed row |

Honesty slice is the committed `eaa7f25` section (this fire's Status/thread writes do not touch it). Hash of that slice after the Status write equals the committed slice.

**Cited by the gym-pin face and not in `WATCHED`:**

| File | SHA-256 |
|---|---|
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/evidence_bar.py` | `7E7EC20DB47D4F1BB64F364AE1AB2CA41852DD68E0C9F896C0608873B9B26A9D` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/critic.py` | `152CF1C9FC2F5DBDB6932FDAB225EF4D785F0C0BC05ECE6207A005CC83000477` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/watch.py` | `44DFC0036F75C3E369A713942295D0580A9862739201AF8C9D278994284710D0` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/paths.py` | `C543D98DB589C9822DA73F712C03A2EC22F0E3BDCFF6F5EA5630B4E225CD476C` |
| `golf-offshoot/docs/HONER_15M_PROMOTION.md` | `B85E34BE8351DB75197BDAAD1CE682D607011F87853086B9CB0A1CF874FBDD72` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/consult_honer.py` | `D4698C7A86D94B8380FBCFB9283A795287D650FD2D63A3F6048F6E07C41E98D3` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/paper.py` | `F52AB86F81793482543C0E70180CD5F76E9F000EA5BD0B10B3D467597FF68447` |
| `golf-offshoot/docs/LEARNING_LANE_15M_LAB_PROPOSED_02.md` | `B658C4CC45AA131163426D5D4E15ADEC0E71C41C12A6E206050E0900C6E960BB` |
| `golf-offshoot/docs/LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_02.md` | `3392E8F415D169CAB3A6A2D545CC85D4C598F830A43E187ADE0FD2780E21AC88` |
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR_CRITIC_13.md` | `206017108A45FCBC8CBDC77F60191C9E2C1A1ECEC8200326E32AD862CC5D356C` |
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR_OPERATOR_ANSWER_12.md` | `330BF435A7EAA9A168D244C47669EDC9A77B041470957E17F659CD98FF71D4BF` |

**Honer catalog/rules starvation hashes — remain after this attack. Not attacked.**

| File | SHA-256 |
|---|---|
| `golf-offshoot/docs/HONER_15M_CATALOG.json` | `CFAF1E50975B230B17103C16170C282F123FB842E349AE90990AC51C2AD220D6` |
| `golf-offshoot/docs/HONER_15M_RULES.json` | `E6EF7CEFCF300B14A1B5FC3F66F79F136121317E07728912AE568367F30C7C3E` |
| `golf-offshoot/docs/HONER_15M_EVIDENCE_BAR.md` | `2AC5D500B40B00784DD3D1C531A464FCAA667F377B0B56E60935D1BE87F83384` |
| `golf-offshoot/docs/HONER_15M_EVIDENCE_BAR.json` | `0C510965AE3A0828E037C6C3AD5D12FE7B167AE6D20AD795383D12D7AA679905` |

No `honer_consult.json`, `series_fee.json`, or `fee_schedule_probe.json` on this tree under `golf-offshoot/data/learning_lane_15m/latest/` or `/workspace/kalshi_15m_exports/latest/` (`/workspace` exists; the export root does not). I did not call `artifact_root_15m()` / `latest_dir_15m()` and did not create that directory.

Hashes recomputed at the end of the attack and unchanged: the text I attacked did not move under me.

---

## Posture

I did **not** open a window outcome file — no `paper/*.json`, no `settlements/*.json`, no `latest/journal.json`, no `rule_decisions.json`. I did not call `score_rule`, `write_critic_findings`, `gym_fee_tick`, `latest_dir_15m`, or `mark_roles_served`. I did not run the digest generator. I did not start or stop a hub. I did not fetch `https://kalshi.com/docs/kalshi-fee-schedule.pdf`. `watch.json` is not on this tree.

The package import pulls `pydantic`, which is not installed. Numbers below come from the bar, `critic.py` / `evidence_bar.py` / `paths.py` / `watch.py` as text, the fee-probe file, the commit record, and a standalone copy of `check_series_fee_regime_matches`'s pass condition (injected snapshots; no package import; no mkdir).

I did not edit the bar, the JSON, the registry, `consult_honer.py`, `critic.py`, `evidence_bar.py`, `DESK.md` (except the worker Status/thread this fire is required to write), or `AGENT_LEAVE_OFF.md` until closeout. I did not set `binding: true`. I did not pin a fee-schedule hash. I did not write a placeholder. I did not set `consult_enabled`.

---

# The one-sentence version

**The gym-pin face says `snapshot_absent` is a named half-pass and not a silent green, and it labels the probe/snapshot as `latest/*.json`; the eighth check returns `state: PASS` on absence (and on an unreadable present file), is inside `CHECKS` so it will not block `passed: true` once the fee hash pins, and the labeled `latest/` path is the same fallback shape ANSWER 11 already had to name for `honer_consult.json`.**

---

## What I accept as closed on the prior text (not re-filed)

CRITIC 12's three UPHELD were recorded on the ANSWER 12 hashes. CRITIC 13 found zero residue on those hashes. I am not re-opening them as if unanswered. I hunted residue that would be **new on these gym-pin bytes**.

| Prior sentence | Gym-pin change I verified on these hashes | Closed as to the prior sentence? |
|---|---|---|
| CRITIC 12 / ANSWER 12 destination-compositor face | MD `:29–37` and JSON `:13–45` still carry in-memory `consult` tag, flag-write leaves dark / raises without theta, missing/null δ is 0 under `H-SKIP-WIDE-SPREAD`. `amendment` says that face remains. | **Yes.** Not re-filed. |
| 429 recorded; sha256 empty; no placeholder | JSON `:271–277` still empty sha256, status 429, checked_at `2026-09-09T17:01:17.703851-04:00`. Probe `5DD35C25…` agrees. `apply_fee_schedule_pin` (`evidence_bar.py:312–345`) returns False unless status 200 and 64-hex. I did not fetch the PDF. | **Yes, as to the standing fail and the no-placeholder rule.** Residue of "429 off the bar" / gym path is objections 1–2. |
| `fee_adjust` reads `fee_hurdle.k`; live M does not retune k | `fee_k` / `fee_adjust` (`evidence_bar.py:104–163`) still read `fee_hurdle.k` only. `write_series_fee_snapshot` note says the same. | **Yes.** |
| A present snapshot with missing field or drifted M FAILs | Standalone copy: omitted `fee_multiplier_present` → FAIL; M 0.5 vs expected 1 → FAIL. Tests `test_series_fee_mismatch_and_missing_field_fail` assert the same. | **Yes, as to a well-formed present snapshot.** Residue of absence / unreadable is objection 1. |
| Registry / consult / binding | Digest `BBD87E52…`; `trials_to_date` 1; one log row, kind `declaration`; `R-SKIP-2TO1-FAVORITE.execution` true; `R-SKIP-COINFLIP.execution` false; `verifiably_preregistered` false on both selection rows. `binding` false. Consult off. I did not enable it. | **Yes.** |

---

## Objection 1 — `snapshot_absent` is `state: PASS` and will not block a suite green

### **UPHELD.**

The gym-pin face (`LEARNING_LANE_15M_EVIDENCE_BAR.md:47`):

> No snapshot yet is a named `snapshot_absent` half-pass — not a silent green, not a pin of k. A present snapshot with a missing field or a drifted M **FAIL**s.

JSON `:280` is the same sentence. `series_fee_regime_matches` is in `CHECKS` (`critic.py:890–899`). `honesty_stamp_is_fresh` was moved to `DESK_CHECKS` (`:901–907`) specifically so a named non-bar property would **not** set `passed`.

`check_series_fee_regime_matches` (`critic.py:787–798`):

```
snap = load_series_fee_snapshot(root=root)
if not snap:
    return _check(
        "series_fee_regime_matches",
        ...,
        True,
        "snapshot_absent — PaperWatch has not written latest/series_fee.json; …",
        {"snapshot_absent": True, …},
    )
```

`_check` (`:148–155`) sets `state` to `PASS` when the third argument is `True`. `run_critic_invariants` (`:936–950`) builds `failing` from `state != PASS` and sets `passed: not failing`. `snapshot_absent` therefore does **not** appear in `failing` and does **not** block `passed: true`.

Standalone copy of that pass condition (no package import): missing snap → `{state: PASS, snapshot_absent: True}`; `{}` → same PASS; a well-formed omitted-M or drifted-M snapshot → FAIL; a matching snapshot → PASS. The face's "present missing/drifted FAIL" half is true. The "not a silent green" half is false of the machine: the check is a suite PASS on absence.

Binding condition 2's first branch is `critic-invariants` passing on the bytes proposed to bind. Today `fee_schedule_hash_recorded` still fails, so the suite stays red. Once a gym 200 pins `schedule_sha256`, `snapshot_absent` will not keep `passed` false. A suite can then go green without a live `fee_type` / `fee_multiplier` ever having been ingested.

`load_series_fee_snapshot` (`evidence_bar.py:393–401`) returns `{}` on a missing file, unreadable JSON, or a non-dict payload. Those three also hit `if not snap` and return the same PASS with the detail "PaperWatch has not written latest/series_fee.json". A present unreadable file is therefore reported as "no snapshot yet" and greens, while the face says a present snapshot FAILs.

I am not proposing a default. I did not write a snapshot. I did not call `latest_dir_15m()`.

**Concrete change demanded.** State that `snapshot_absent` returns `state: PASS`, is inside `CHECKS`, and will not appear in `failing` — so it will not block `passed: true` once `fee_schedule_hash_recorded` clears — **or** return FAIL / move the check to `DESK_CHECKS`. State that an unreadable or empty present file is the same PASS, not "has not written." Do not pin a hash. Do not fetch the PDF.

**What would prove me wrong.** Show the absent-snapshot branch passes `ok=False`, or show the check is in `DESK_CHECKS`, or show `run_critic_invariants` treats `evidence.snapshot_absent` as failing. The copy I read returns `True` and is the eighth `CHECKS` member. Show `load_series_fee_snapshot` on invalid JSON is distinguished from missing. The function I hashed returns `{}`.

---

## Objection 2 — labeled `latest/*.json` is the fallback shape, not the resolved path

### **UPHELD.**

The gym-pin face (`LEARNING_LANE_15M_EVIDENCE_BAR.md:49`):

> a 429 is written to `latest/fee_schedule_probe.json` and does **not** amend this bar

JSON `:277` / `:280` and the eighth-check detail (`critic.py:792`) name `latest/fee_schedule_probe.json` and `latest/series_fee.json` the same way. ANSWER 11 already had to name both resolved paths for `honer_consult.json` after CRITIC 11 objection 2. These two new gym files reuse the unlabeled shape.

`fee_probe_path` / a no-arg `series_fee_snapshot_path` (`evidence_bar.py:169–195`) write `latest_dir_15m() / <name>`. `latest_dir_15m` (`paths.py:89–92`) is `artifact_root_15m() / "latest"`. `artifact_root_15m` (`:56–68`) returns `/workspace/kalshi_15m_exports` when `/workspace` exists and the mkdir succeeds, else `golf-offshoot/data/learning_lane_15m`.

`/workspace` exists on this VM. `/workspace/kalshi_15m_exports` does not. I did not call `artifact_root_15m()` and did not create it. The resolved destination *if* the gym ran here is `/workspace/kalshi_15m_exports/latest/fee_schedule_probe.json` and `…/series_fee.json`. The repo fallback is `golf-offshoot/data/learning_lane_15m/latest/…`. Neither file exists at either candidate path. The committed 429 sits in `golf-offshoot/docs/LEARNING_LANE_15M_FEE_SCHEDULE_PROBE.json` (`5DD35C25…`), which is not `latest/`.

PaperWatch `gym_fee_tick(ingest)` (`watch.py:228–229`) passes no `root` / `latest_dir`, so it writes `latest_dir_15m()`. Live `write_critic_findings()` (`runner.py:562–565`) also passes no root, so the eighth check reads `latest_dir_15m()`. Those two agree with each other. They do not agree with the labeled `latest/` token, and they do not agree with `check_series_fee_regime_matches(root=<repo>)`, which reads `root / golf-offshoot/data/learning_lane_15m/latest/series_fee.json` (`evidence_bar.py:184–191`). A caller that passes `root` looks at the fallback while the gym writes the export root.

The destination paragraph already knows how to say this for honer. The gym-pin sentences do not.

**Concrete change demanded.** State the two resolved paths for `fee_schedule_probe.json` and `series_fee.json` the same way `:29` now names both `honer_consult.json` paths: `/workspace/kalshi_15m_exports/latest/…` when `/workspace` exists, else the repo fallback. State that a `root=` critic call reads the repo path. Do not create the export root. Do not write a snapshot. Do not fetch the PDF.

**What would prove me wrong.** Show the gym-pin face names both resolved probe/snapshot paths, or show `fee_probe_path` / no-arg `series_fee_snapshot_path` do not use `latest_dir_15m()`. The functions I hashed do.

---

## Objection 3 — the ratchet paragraph still says seven method checks

### **UPHELD.**

The gym-pin face (`LEARNING_LANE_15M_EVIDENCE_BAR.md:47`) says the method suite has **eight** checks and names `series_fee_regime_matches` as the eighth. `critic.py:890–899` `CHECKS` has eight members. The same hashes, `:305`:

> The suite still has seven method checks; several were **rewritten** at `0a480d4` rather than appended.

JSON `ratchet_guards` (`:358–372`) has no row for the eighth check. The ratchet paragraph exists because CRITIC 01 / Y2 was "zero new `CHECKS` members." These bytes added a `CHECKS` member and left the paragraph that counts them on seven.

I am not asking for a new mechanical check. I am asking the face that just added the eighth check not to also say there are seven.

**Concrete change demanded.** Make `:305` say eight, and add a `ratchet_guards` row that names `series_fee_regime_matches` (including that `snapshot_absent` is `state: PASS` if objection 1 is sustained). Do not score. Do not bind.

**What would prove me wrong.** Show `:305` says eight, or show `ratchet_guards` names `series_fee_regime_matches`. The hashes I attacked say seven and omit the row.

---

# Considered and not filed

- **CRITIC 12 items 1–3 as if unanswered.** The demanded face-statements remain on these hashes. Not re-filed.
- **The historical 429 still sitting on `fee_hurdle`.** Disclosed (`schedule_fetch_status` 429 at 17:01:17). "429 off the bar" is the future-probe claim (`apply_fee_schedule_pin` leaves bytes alone unless 200 + 64-hex). Not a second 429 finding.
- **Unpinned fee hash / the 429 itself.** Disclosed. Standing named fail. I will not write a placeholder. I will not fetch the PDF.
- **`apply_fee_schedule_pin` on a 200 rewrites the bar from PaperWatch.** Intended pin. Named. Not an objection.
- **`expected_fee_type` / `expected_fee_multiplier` default to quadratic / 1 when omitted** (`critic.py:780–785`). Live bytes have both keys. Neighbor of objection 1's fail-open; filing a second default would pad.
- **`fee_probe_due` re-arms if the probe file is missing.** Cooldown state lives in that file. Neighbor of "every 12 hours." Not filed.
- **`_fee_tick` swallows exceptions and returns None.** Named "never takes the paper loop down." Not a face lie.
- **12h cooldown / UA / 429-does-not-pin / later-429-keeps-hash.** Tests `test_a_429_does_not_amend_the_bar`, `test_a_200_pins_and_a_later_429_keeps_the_hash`, `test_fee_probe_cooldown_skips_a_second_fetch` hold those sentences. Not filed.
- **`WATCHED` still five factory artifacts.** Disclosed. Probe / series_fee / PROPOSED 02 notes unpaid, not new residue.
- **AND-skip / in-memory consult tag / flag-write / missing δ.** ANSWER 12 face remains. Not re-filed.
- **`WATCHED.lab_proposed` frozen on `_01`.** Disclosed, unpaid, CRITIC 03. Not re-filed.
- **`favorite_odds=2` / clause-(2) leftover ordinals / keyword greens / δ-check / vacuous greens.** Prior critics; already answered or disclosed. Not re-filed.
- **Honer catalog/rules starvation (`86e0cce`).** Job says those hashes remain after. Recorded, not attacked.
- **Copied `delta_above_detection_floor` still fails `alpha_first_look` vs current α_k.** Named on the face; owed to Systems. Not re-filed.
- **X2 lived honoring unproven / `currently_reachable` false.** Already answered. No new verb.
- **Scoring, eligible counts, opening the tape.** Forbidden this Job.
- **Enabling consult, binding, arming, lifting HOLD, reviving `R-SKIP-COINFLIP`, probing the fee PDF.** Forbidden this Job.

---

**Closing.** I proposed no rule, scored no rule, ADMITted nothing, PARKed nothing, and did not Soften, Harden or Kill anything. I did not set `binding: true`, did not pin a fee-schedule hash, and did not write a placeholder for one. I did not enable consult. I opened no window outcome file. I did not fetch the PDF. I did not edit the bar, the registry, the compositor, or `critic.py`. Honer catalog/rules hashes remain unreviewed. Trading is **NOT ARMED**.

Three objections, all UPHELD. Condition 1 stays unmet for these bytes until Operator records this attack in a separate turn. Binding stays `false` as I found it. Conditions 2 and 3 are not met on these bytes.

Operator answers these. Handoff → `operator`.
