# Soften Critic — attack on Systems 16:50 destination/compositor/fee-retry factory bar (CRITIC 11)

**Role:** Soften Critic · **Date:** 2026-09-09 · **Lane:** `learning_lane_15m` · series `KXBTC15M` only
**Branch:** `cursor/honer-15m-sibling` at `a19b5b6` (CoS assign). Factory bar and compositor last touched at `1b21a70` (16:50 destination alignment). Git blobs of the two attacked bar files still equal `1b21a70` (`448dc5a7…` / `8dd62887…`).

**Session separation.** This session did not draft the bar, did not write CRITIC 01–10, did not write ANSWER 01–09, did not land `1b21a70` / `86e0cce`, did not declare `R-SKIP-2TO1-FAVORITE`, and did not RUN-ONLY it. `PROTOCOL.md` forbids one turn both objecting and dismissing.

**This is an attack, not a verdict.** Operator answers each objection in a turn that is not this one. Nothing here is a Soften, an ADMIT, a RUN-ONLY, a park, a score, or a proposal. No rule was scored. `R-SKIP-COINFLIP` was not revived and its band was not retuned. Consult was not enabled. `binding` stays `false` exactly as I found it. Trading is **NOT ARMED**.

**Why this file exists.** Desk Job 2026-09-09 18:15 ET: attack the Systems 2026-09-09 16:50 destination/compositor/fee-retry factory evidence-bar bytes (honer as discovery organ; dark AND-skip consult named and off; fee-schedule probe retry). Written objections only. Honer catalog/rules starvation hashes remain after this attack.

---

## Artifacts attacked, by SHA-256

Computed independently via the same newline-normalised `read_text` → UTF-8 digest `critic.artifact_digest` uses (copied, not imported: this tree's package import pulls `pydantic`, which is not installed in this fire). Byte hash equals the normalised hash on every file below (0 CRLF pairs), except the honesty section, which is a slice of `DESK.md`.

| File | SHA-256 (bytes = normalised) | Last findings hash | Unreviewed? |
|---|---|---|---|
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR.md` | `9C323D59CA4EBCDA1CDB97D040C6DFD7D859B84409D5F29973D1DDECEEBA2D0E` | `5F2AA5F5…` | **Y** — 16:50 destination bytes; git blob `448dc5a7…` = `1b21a70` |
| `golf-offshoot/docs/LEARNING_LANE_15M_EVIDENCE_BAR.json` | `645A82F890B23C37FA9F5214F45D9C2BB09A16F33F1D23176515A0396E8FEB65` | `2611C255…` | **Y** — same 16:50 gap; git blob `8dd62887…` = `1b21a70` |
| `golf-offshoot/docs/LEARNING_LANE_15M_RULES.json` | `BBD87E52EE6FC1E5F797C676EA6038F4C77404DDE7F29E21128BD75BF4B802D0` | `CD25DD72…` | **Y** — last factory-rules touch `3c89a7f`; not edited at `1b21a70` |
| `docs/agents/DESK.md` `## Honesty checklist` | `3177E87DD3E1766EADBA0BAE73947DE7CA1D4CB7B0FBCBAE89C54BBFBBD07B72` | `07055605…` | **Y** — restamp names the 16:50 assign |
| `golf-offshoot/docs/LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_01.md` | `83D4463B4BB55D327485E3874A4BF3DAA54727D7ED37F56C124C6E8913DAF0B4` | `83D4463B…` | N — still the watched `lab_proposed` path |
| `golf-offshoot/docs/LEARNING_LANE_15M_FEE_SCHEDULE_PROBE.json` | `5DD35C25BADDF941803558D4D7DF871D4BA5CE5620588CE895DDFC7D0CCCC36C` | — | not `WATCHED`; cited by the 16:50 fee retry |

Honesty slice is the committed `a19b5b6` section (this fire's Status/thread writes do not touch it).

**Cited by the 16:50 face and not in `WATCHED`** (new destination files; CRITIC 03 objection 5 on `_01` is disclosed and not re-filed):

| File | SHA-256 |
|---|---|
| `golf-offshoot/docs/HONER_15M_PROMOTION.md` | `B85E34BE8351DB75197BDAAD1CE682D607011F87853086B9CB0A1CF874FBDD72` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/consult_honer.py` | `D4698C7A86D94B8380FBCFB9283A795287D650FD2D63A3F6048F6E07C41E98D3` |
| `golf-offshoot/src/golf_offshoot/learning_lane_15m/paper.py` | `F52AB86F81793482543C0E70180CD5F76E9F000EA5BD0B10B3D467597FF68447` |
| `golf-offshoot/docs/LEARNING_LANE_15M_LAB_PROPOSED_02.md` | `B658C4CC45AA131163426D5D4E15ADEC0E71C41C12A6E206050E0900C6E960BB` |
| `golf-offshoot/docs/LEARNING_LANE_15M_OPERATOR_NOTE_PROPOSED_02.md` | `3392E8F415D169CAB3A6A2D545CC85D4C598F830A43E187ADE0FD2780E21AC88` |

**Honer catalog/rules starvation hashes — remain after this attack. Not attacked.**

| File | SHA-256 |
|---|---|
| `golf-offshoot/docs/HONER_15M_CATALOG.json` | `CFAF1E50975B230B17103C16170C282F123FB842E349AE90990AC51C2AD220D6` |
| `golf-offshoot/docs/HONER_15M_RULES.json` | `E6EF7CEFCF300B14A1B5FC3F66F79F136121317E07728912AE568367F30C7C3E` |
| `golf-offshoot/docs/HONER_15M_EVIDENCE_BAR.md` | `2AC5D500B40B00784DD3D1C531A464FCAA667F377B0B56E60935D1BE87F83384` |
| `golf-offshoot/docs/HONER_15M_EVIDENCE_BAR.json` | `0C510965AE3A0828E037C6C3AD5D12FE7B167AE6D20AD795383D12D7AA679905` |

No `honer_consult.json` on this tree under `golf-offshoot/data/learning_lane_15m/latest/` or `/workspace/kalshi_15m_exports/latest/` (the export root does not exist on this VM). I did not call `artifact_root_15m()` and did not create that directory.

Hashes recomputed at the end of the attack and unchanged: the text I attacked did not move under me.

---

## Posture

I did **not** open a window outcome file — no `paper/*.json`, no `settlements/*.json`, no `latest/journal.json`, no `rule_decisions.json`. I did not call `score_rule`, `write_critic_findings`, or `mark_roles_served`. I did not run the digest generator. I did not start or stop a hub. I did not fetch `https://kalshi.com/docs/kalshi-fee-schedule.pdf`. `watch.json` is not on this tree.

The package import pulls `pydantic`, which is not installed. Numbers below come from the bar, the compositor and `paths.py` as text, the promotion protocol as text, the fee-probe file, the commit record, and arithmetic I can do without the tape. `express_frozen_honer` / `consult_is_enabled` / `compose_and_skip` were re-run from a standalone copy of those functions, not by importing the package.

I did not edit the bar, the JSON, the registry, `consult_honer.py`, `critic.py`, `DESK.md` (except the worker Status/thread this fire is required to write), or `AGENT_LEAVE_OFF.md` until closeout. I did not set `binding: true`. I did not pin a fee-schedule hash. I did not write a placeholder. I did not set `consult_enabled`.

---

# The one-sentence version

**The 16:50 face names a Founder-gated dark AND-skip destination; the compositor it cites enables on an unwatched, dual-root JSON flag, expresses two unnamed families (the spread family still applies the rich-YES θ skip), and would attribute a honer skip to the factory `rule_id` — and the cited exam contrast is not this bar's fee-adjusted test.**

---

## Objection 1 — "Founder-named implement" is not a machine gate

### **UPHELD.**

`LEARNING_LANE_15M_EVIDENCE_BAR.md:31`: "Enabling is a Founder-named implement after exam + later-session Critic." JSON `discovery_organ.consult_enabled` is `false`. Protocol step 8: "Founder names implement — the only enable."

The compositor the same paragraph cites has no Founder field. `consult_honer.py:39–40`:

```
def consult_is_enabled(snapshot: dict[str, Any] | None) -> bool:
    return bool(snapshot) and snapshot.get("consult_enabled") is True
```

`compose_and_skip` (`:91–93`) is dark only while that predicate is false. There is no committed lock, no env, no Founder token, no check that exam + later-session Critic happened. A snapshot dict with `consult_enabled` exactly `true` is sufficient. I recomputed: `consult_is_enabled({"consult_enabled": True})` is true; `"true"` (string) is false — the exact-True half of the bar is right; the Founder half is not in the function.

The snapshot path is `latest_dir_15m() / "honer_consult.json"` (`consult_honer.py:17–25`, `paths.py:89–92`). `latest_dir_15m` mkdir's `artifact_root_15m() / "latest"`. That root is **not** a committed docs file. On a machine where `/workspace` exists, `paths.py:61–64` prefers `/workspace/kalshi_15m_exports` — outside this git tree. On the Windows fallback it is `golf-offshoot/data/learning_lane_15m/latest/`, which is the lane data dir (gitignored). Either way, writing `consult_enabled: true` into that file moves Lineage A on the next `consult_registry` call (`paper.py:269–274`) with **no git change**.

`critic.py:66–72` `WATCHED` is still the five factory artifacts. It does not include `consult_honer.py`, `HONER_15M_PROMOTION.md`, or `honer_consult.json`. `artifact_unreviewed` cannot fire on the enable flag.

**Currently off is true on this VM this turn** — no snapshot file at either candidate path; I did not create one. I am not claiming consult is on. I am claiming the bar's "Founder-named" sentence is policy sitting on top of a file flag.

**Concrete change demanded.** Either (a) state on the factory bar face that "Currently off" is a missing-or-false `consult_enabled` in the resolved `latest/honer_consult.json` and that writing that flag is sufficient to move Lineage A — Founder-named is not a machine gate — or (b) name a committed lock as owed before the destination paragraph can be read as a gate. Do not enable consult in the answer turn. Do not treat this as a request to flip the flag.

**What would prove me wrong.** Show `compose_and_skip` requires something other than `snapshot.consult_enabled is True` (a committed lock, a Founder field, a watched enable artifact). The function I hashed does not.

---

## Objection 2 — The labeled snapshot path is not the path the compositor reads when the external root wins

### **UPHELD.**

JSON `discovery_organ.consult_snapshot` is `"learning_lane_15m/latest/honer_consult.json"` (`LEARNING_LANE_15M_EVIDENCE_BAR.json:17`). The MD says `` `latest/honer_consult.json` `` (`:29`). The protocol says the same lane-shaped path (`HONER_15M_PROMOTION.md:11`).

The compositor reads `latest_dir_15m() / SNAPSHOT_NAME` (`consult_honer.py:23–25`). `artifact_root_15m()` (`paths.py:56–68`): if `EXTERNAL_15M_ROOT.parent.is_dir()` — `/workspace` — mkdir and return `/workspace/kalshi_15m_exports`. Then latest is `/workspace/kalshi_15m_exports/latest/honer_consult.json`. That path has **no** `learning_lane_15m` component.

`/workspace` is a directory on this VM. I did not call `artifact_root_15m()` (it mkdir's). The preference is in the source: `:61` `if EXTERNAL_15M_ROOT.parent.is_dir()`.

This is the same dual-root the leave-off already treats as the cause of two paper lineages. The 16:50 bar put the consult switch on that resolver and labeled only the fallback shape.

A reader who writes `golf-offshoot/data/learning_lane_15m/latest/honer_consult.json` on a `/workspace` machine is not writing the file `compose_and_skip` loads. A reader who looks for the labeled path will not find the file that would actually move Lineage A.

**Concrete change demanded.** Name both resolved locations on the factory bar face: external `/workspace/kalshi_15m_exports/latest/honer_consult.json` when `/workspace` exists, else `golf-offshoot/data/learning_lane_15m/latest/honer_consult.json`. Do not leave `learning_lane_15m/latest/honer_consult.json` as if it were one path.

**What would prove me wrong.** Show `latest_dir_15m()` on a tree where `/workspace` exists returns a path containing `learning_lane_15m/latest/honer_consult.json`. The source returns `kalshi_15m_exports/latest`.

---

## Objection 3 — The spread family still applies the rich-YES θ skip; neither family is on the factory bar face

### **UPHELD.**

The 16:50 paragraph says the compositor "may add a skip from a **frozen snapshot**" (`LEARNING_LANE_15M_EVIDENCE_BAR.md:29`). It does not name `H-SKIP-RICH-YES` or `H-SKIP-WIDE-SPREAD`. Those strings are the only families `consult_honer.py:19–20` knows.

`express_frozen_honer` (`:75–79`), after the spread branch, still applies `posted_yes >= theta` for **every** family:

```
if family == FAMILY_SPREAD and spread is not None and float(spread) >= delta:
    return "skip", ...
if float(posted_yes) >= theta:
    return "skip", ...
return "fill", ...
```

Standalone recompute (same branches):

| family | posted_yes | spread | θ | δ | result |
|---|---|---|---|---|---|
| `H-SKIP-WIDE-SPREAD` | 0.80 | 0.01 (tight) | 0.75 | 0.03 | **skip** — `posted_yes >= theta` |
| `H-SKIP-WIDE-SPREAD` | 0.40 | 0.01 (tight) | 0.75 | 0.03 | fill |
| `H-SKIP-WIDE-SPREAD` | 0.40 | 0.05 (wide) | 0.75 | 0.03 | skip — spread ≥ δ |

`tests/test_consult_honer.py:72–82` sets θ = 0.99 so the rich-YES gate cannot fire in that test. The OR is untested there and unnamed on the factory bar.

A surviving exam that freezes `H-SKIP-WIDE-SPREAD` therefore still skips rich YES even on a tight book. That is a second skip region inside a family the destination paragraph does not name. I am not proposing a third family and I did not touch honer catalog/rules.

**Concrete change demanded.** Name both families the compositor already expresses, and state that `H-SKIP-WIDE-SPREAD` is OR(spread ≥ δ, posted_yes ≥ θ), not spread-only. Do not enable consult. Do not retune θ/δ.

**What would prove me wrong.** Show `express_frozen_honer` with `family=H-SKIP-WIDE-SPREAD`, spread < δ, posted_yes ≥ θ returns fill. The copy I ran returns skip.

---

## Objection 4 — A honer skip keeps the factory `rule_id` and replaces the reason

### **UPHELD.**

`compose_and_skip` on a factory fill + honer skip (`consult_honer.py:103–107`):

```
out = dict(factory_verdict)
out["action"] = "skip"
out["reason"] = honer_reason
out["consult"] = "honer_and_skip"
```

`rule_id` is copied, not replaced. Standalone recompute: factory `{rule_id: "R", action: "fill"}` plus a honer skip becomes `{rule_id: "R", action: "skip", reason: "posted_yes >= theta …", consult: "honer_and_skip"}`.

`paper.py:317–334` then records `rule_id: verdict.get("rule_id")` and `reason: verdict["reason"]`. A later reader grouping skips by `rule_id` attributes a honer skip to `R-SKIP-2TO1-FAVORITE` (or whichever factory row expressed **fill**). The factory rule did not skip. The compositor did.

The bar's destination sentence is "one executing factory rule still expresses fill-or-skip" (`:29`). After compose, the skip expression is honer's and the id is still the factory's. Consult is off on this tree, so Lineage A is not currently poisoned. The bug is in the live path the 16:50 face named as the destination.

**Concrete change demanded.** State on the factory bar that a honer skip under this compositor keeps the factory `rule_id` and overwrites `reason`, so a `rule_id` group is not a factory-only skip set — **or** name an attribution field as owed before consult can be read as "one factory rule still expresses." Do not enable consult in the answer turn.

**What would prove me wrong.** Show `compose_and_skip` clears or replaces `rule_id` on a honer skip, or show `paper.py` records the skip under a honer id. The functions I hashed keep the factory id.

---

## Objection 5 — The cited promotion protocol scores a different contrast than this bar

### **UPHELD.**

The 16:50 face cites [`HONER_15M_PROMOTION.md`](HONER_15M_PROMOTION.md) as the destination protocol (`LEARNING_LANE_15M_EVIDENCE_BAR.md:31`, JSON `:20`). Protocol step 6 (`HONER_15M_PROMOTION.md:29`):

> Score — n=70 in-band exam windows, contrast `d_i = pnl_exam − pnl_fill_all`

That contrast has no `fee_adj`, no δ floor, no matched-exposure permutation, no α schedule, and names "in-band exam windows," not this bar's L1 (eligible windows after `declared_at`).

This bar's Established contrast (`LEARNING_LANE_15M_EVIDENCE_BAR.md:89–108`, JSON `:156–231`) is `d_i = pnl_rule_fee_adj_i − pnl_baseline_fee_adj_i`, H0 `mean(d) ≤ δ`, clauses (1)–(5), L1 n=70 then lived L2.

The 16:50 sentence is that a surviving honer exam **may earn** a dated AND-skip consult inside factory `consult_registry`. A skip that survived a gross exam contrast would then sit inside factory `decide()` and be scored, if ever, under this bar's fee-adjusted test. Those are two tests. The factory bar does not say so.

I am attacking the factory face for citing that protocol as the earn-path without naming the contrast mismatch. I did not edit the protocol. I did not score.

**Concrete change demanded.** State on the factory bar that the protocol's exam contrast is `pnl_exam − pnl_fill_all` (gross, in-band) and is **not** this bar's fee-adjusted five-clause test, and that surviving the exam is therefore not surviving this bar. Do not score. Do not enable consult.

**What would prove me wrong.** Show protocol step 6 uses `pnl_*_fee_adj`, δ, clauses (1)–(5), and this bar's L1 definition. It does not.

---

## Objection 6 — `owed_to_systems` still names the 09-08 13:42 429 after the 16:50 retry

### **UPHELD.**

The 16:50 amendment's claimed fee work is the retry. Face (`LEARNING_LANE_15M_EVIDENCE_BAR.md:43`) and `fee_hurdle` (`LEARNING_LANE_15M_EVIDENCE_BAR.json:246–248`) name HTTP 429 at `2026-09-09T17:01:17-04:00`. The probe file I hashed (`LEARNING_LANE_15M_FEE_SCHEDULE_PROBE.json`) agrees: `checked_at` that stamp, `status` 429, `sha256` `""`, `bytes` 0, note "No placeholder was written."

The same JSON's `owed_to_systems` row (`:352`) still reads:

> pin the Kalshi fee schedule sha256 (external; last fetch HTTP 429 at 2026-09-08T13:42:26-04:00)

That is the **prior** 429, not the retry this amendment recorded. The machine copy's owed list is a stale picture of the one new fee fact the 16:50 turn added.

I do not file the empty sha256 as a new objection. Condition 2 already names it. Recording another 429 does not pin a hash. I did not fetch the PDF.

**Concrete change demanded.** Restate that owed row to the 17:01:17 retry, or strike the dated clause so the owed list cannot be read as the last fetch. Do not write a placeholder sha256.

**What would prove me wrong.** Show `owed_to_systems` already names `2026-09-09T17:01:17`. It names `2026-09-08T13:42:26`.

---

# Considered and not filed

- **Unpinned fee hash / the 429 itself.** Disclosed on the face as the standing condition-2 blocker. Probe sha256 is empty. I will not write a placeholder. I will not fetch the PDF.
- **AND-skip: factory skip stays skip; honer fill cannot force a fill.** Verified from `compose_and_skip` `:96–97` and a standalone recompute. The bar is right. Not an objection.
- **`consult_enabled` exactly `true`.** String `"true"` stays dark. The bar's "exactly true" is right. Filed only the Founder-gate half (objection 1).
- **Live wandering θ never consults.** `consult_honer.py` does not import `honer_15m` and does not read `theta.json`. `tests/test_consult_honer.py:163–170` asserts that. Accepted.
- **Dark path returns the same object.** `compose_and_skip` `:92–93`; test `:27–31`. "Lineage A is bit-identical to `rules.decide()` alone" holds while the snapshot is absent or the flag is not exact True. Accepted on this tree this turn.
- **`WATCHED.lab_proposed` frozen on `_01`.** Disclosed, unpaid, CRITIC 03. Not re-filed. The `_02` hashes are recorded above.
- **`favorite_odds=2` / clause-(2) leftover ordinals / keyword greens / δ-check / vacuous greens.** Prior critics; already answered or disclosed. Not re-filed.
- **Honer catalog/rules starvation (`86e0cce`).** Job says those hashes remain after. Recorded, not attacked.
- **`amended_at` 16:50 vs probe 17:01.** The face discloses both stamps. The sharper inconsistency is the owed row (objection 6). Not a second timestamp finding.
- **`latest_dir_15m` mkdir side-effect on a dark load.** Creates a directory; does not change the paper book. Not filed.
- **Scoring, eligible counts, opening the tape.** Forbidden this Job.
- **Enabling consult, binding, arming, lifting HOLD, reviving `R-SKIP-COINFLIP`.** Forbidden this Job.

---

**Closing.** I proposed no rule, scored no rule, ADMITted nothing, PARKed nothing, and did not Soften, Harden or Kill anything. I did not set `binding: true`, did not pin a fee-schedule hash, and did not write a placeholder for one. I did not enable consult. I opened no window outcome file. I did not edit the bar, the registry, the compositor, or `critic.py`. Honer catalog/rules hashes remain unreviewed. Trading is **NOT ARMED**.

Six objections, all UPHELD. Condition 1 stays unmet for these bytes until Operator records this attack in a separate turn. Binding stays `false` as I found it. Conditions 2 and 3 are not met on these bytes.

Operator answers these. Handoff → `operator`.
