# L1 ADMIT freeze (LOCK-005)
C = trailing-90-day NPS (standard promoter−detractor formula); H = 50; legs = completed surveys only (exclude employees); scope = single named product line. Eval freeze only — ≠ should-not-expand. Amb −4.
