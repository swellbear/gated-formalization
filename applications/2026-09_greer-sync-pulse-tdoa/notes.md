# Notes

**Status:** see [`STATUS.md`](STATUS.md). **2026-09-05:** Founder CLAIM LOCK opens Greer-style sync-pulse TDOA Amb scaffold. Same day: Method Operator **ADMIT** first-pulse fog naming (C1/C2/C3 SUCCEED; sim-only; hardware X PARKED). Same day: Operator **ADMIT #0 HARDEN** (Chan 1994; 1 ns median 0.361 m; 1 ns p90 ≈ 1.16 m; sim X = 0.50 m median-not-p90). Same day: Operator **ADMIT MULTIPATH1 Soften** (Kill not triggered). Frozen Chan 1994; σ_t=1 ns; positive range-bias; same refs/L-path as #0. Baseline 0.364 m; random_k=1 b=0.5 → 0.476; epoch_f=0.25 b=1 → 0.452; strong persistent b≥1–2 m → 0.73–4.7+ m. Sim X = 0.50 m remains LOCKED with NLOS scope annotation. Do not claim multipath-robust 0.50 m. No fingerprint rescue. X remains median-not-p90 (p90≈1.16 m @1 ns LOS). Hardware X PARKED. Same day: Operator **ADMIT SYNC1 Soften** (Kill not triggered). Frozen Chan 1994; σ_t=1 ns; same refs/L-path. σ_sync≲0.3 ns → median 0.382 m ≤ X; σ_sync=1 ns scrapes 0.513 m; ≥3 ns / 3 ns path drift fails X. Same day: Operator **ADMIT JOINT1 Soften** (Kill not triggered; Aim A partial). Path-shared joint clocks (shared-τ) under fixed_trial σ_sync. Restores X up to ≲ 3 ns (0.231 @1 ns; 0.439 @3 ns ≤ 0.50 m); Chan scrape at 1 ns restored. σ_sync=10 ns fails (1.816 m). Drift 3 ns/path still broke X on that pulse (JOINT1 0.919 — shared-τ misspecified vs ramp). Named sync Soften budget widened to σ_sync ≲ 3 ns under JOINT1 + prior mild-NLOS. Same day: Operator **ADMIT DRIFT1 HARDEN** under the named budget. Batch path-shared τ + linear α restores median ≤ 0.50 m on SYNC1 drift breakers (drift=3 @σ=0 → 0.221 m; drift=10 → 0.223 m; α̂ recovers). JOINT1 Soften (σ_sync≲3 ns fixed offsets) still stands. Honesty: path-shared batch model, not free per-epoch realtime; not multipath-robust; not hardware. Same day: Operator **ADMIT GATE1 Soften** (Kill not triggered; aim B Succeed). Detect-only refuse OR (G1a residual ∨ G1b raw LORO). FA σ≤3 drift0 ≈ 0.100; FA +matched drift3 ≈ 0.080; TD σ=10 ≈ 0.828; TD unmatched drift3 = 1.000; TD per_epoch σ=3 = 1.000. Residual-alone misses σ=10; raw LORO carries it. Injection-calibrated → Soften not Harden. Write-up on disk: Founder GREER_WRITEUP.md PRIMARY — HOLD send until user OK. Lab audit GREER_WRITEUP_DRAFT.md. Lab HOLD invent pending Greer criteria / user send OK. Owner-requested collaboration framing; no claim-language copy. GEOM0 HARDEN + MULTIPATH1 Soften + SYNC1 Soften + JOINT1 Soften + DRIFT1 HARDEN stand. Hardware X PARKED. Multipath later. Peek succeed / HARDEN / Soften ≠ claim clearance. Not a locator. Not training. Not skill-met. Not RF fingerprinting. Not GPS/DGPS as the mobile fix. Not a copy of US10135667B1 claims. Not rithm. Cell-tower Amb PARKED — do not reopen as live. BIA→weight portfolio CLOSED — do not reopen. Collatz playground done (#45). Track B paused. llm-gwt R-REPL parked.

**Application:** `2026-09_greer-sync-pulse-tdoa`  
**Date:** 2026-09-05
