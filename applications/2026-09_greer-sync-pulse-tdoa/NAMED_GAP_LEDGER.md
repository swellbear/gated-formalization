# Named-gap ledger — Greer-style sync-pulse TDOA

Habit: [`docs/NAMED_GAP_LEDGER_HABIT.md`](../../docs/NAMED_GAP_LEDGER_HABIT.md). One line per open gap. This is a problem-solving scoreboard, **not** a locator and **not** a product claim.

**Opened:** 2026-09-05 — Founder **CLAIM LOCK** opens a **new** Amb: Greer-style GPS-denied locate via dedicated sync-pulse reference nodes + mobile TDOA. Current framing: owner-requested **collaboration** (bibliographic; US10135667B1 custom-beacon substrate, not a carrier-mast Amb). **No claim-language copy.** Lab proposes first-pulse fog + 2–3 cheap checks. Method Operator gates.

**Last check:** 2026-09-05 — Operator **ADMIT A2 Soften (conditional)** ([`SCORE_A2.md`](SCORE_A2.md); [`DIGESTION_A2.md`](DIGESTION_A2.md)). Soften under **JOINT1 + differential ~2.5 ns (F9T-class) path-shared relative-clock** named. **Kill** bare Chan at ~2.5 ns (~**1.14 m** ≫ X). **Kill** commodity **20–50 ns** / common-view **~10 ns** / absolute-only **~5–15 ns** for **both** curves. **Harden unsupported** (public residuals **ns–tens-of-ns**, **not** `≪ 0.3 ns`). **Brutal lock:** patent simultaneous-via-DGPS-1PPS ≠ commercial 1PPS reality. **A1 Soften carried** (abs ≤1 m poseable, Chan **0.832 m** @ `σ_ref=0.5`; X=0.50 perfect-ref only; RN floor named; do **not** Harden absolute **X**) — [`SCORE_A1.md`](SCORE_A1.md). Copy gate [`COPY_GATE.md`](COPY_GATE.md) **PASS**. Next (user override suite): **GO A3** then **A4**. Write-up = sync-fragility evidence only (**HOLD send** until suite digests).

**What this is not:** This is **not** rithm. Skill-met is **not** claimed. Ingest / Soften / HARDEN is **not** claim clearance, **not** a locator, **not** an absolute 0.50 m, **not** a multipath-robust 0.50 m, and **not** a free per-epoch realtime drift claim. Training is **not** started and is **not** established. Hardware **X** is **not** locked. Fingerprint / ML / RF invent is **not** a rescue. Patent claims are **not** copied. The cell-tower Amb is **PARKED** and is **not** reopened as live. SkyMirr is a **separate** Amb. The BIA→weight portfolio is **CLOSED** and is **not** reopened here.

**Process:** Lab invents; Operator admits, rejects, or parks. Lab does **not** self-admit. After A2 Soften (conditional), **Lab HOLD invent** except **A3 opened**. Write-up is on disk (**HOLD send** until the suite digests). Still **no RF / ML**. **No** fingerprint rescue. **Multipath later.** Hardware **X PARKED**. Link/map **PARKED**.

**Cell-tower geometry** is **PARKED** (Founder STOP / user pivot; peek #61 + X=300 m on record; A→B halted). This app does **not** reopen it as live.  
**SkyMirr MuLCAT** is a **separate** Amb (SURROGATE1 Soften on its own record). This app does **not** reopen it.  
**BIA→weight portfolio** is **CLOSED** (human #59 ship demo + kill of the accurate-weight claim; animal parks stay). This app does **not** reopen it.  
**Collatz playground** is **done** (#45). Lab HOLD there (unchanged).  
**Track B invent** remains **paused** (unchanged).  
**llm-gwt R-REPL** remains **parked** (unchanged).

## Claim line (parent; two bars; hardware X PARKED)

`on a laptop-feasible sim/prototype path, a ≥3-reference simultaneous-sync TDOA estimator (GPS/DGPS used only to place/time refs; never as the mobile fix) recovers mobile position without RF fingerprint training` → **two bars** after abstract ingest + A1 Soften: (1) patent-facing **≤1 m xy** **stands** (poseable under DGPS-class RN survey error; Chan abs **0.832 m** @ `σ_ref=0.5`; **not** claim copy; **not** p90; **not** hardware); (2) **sim X = 0.50 m** is **perfect-ref scoped sim only** (JOINT1 + ~2.5 ns F9T-class path-shared relative-clock + named GEOM0 noise + DRIFT1 / GATE1 / mild NLOS; **median-not-p90**) — **not** the patent promise and **not** an absolute bar under the RN floor. Named **DGPS ~0.4–0.5 m** absolute floor. A2: commercial 1PPS does **not** carry either bar. Kill vs succeed: fail closed if the path is not laptop-feasible **or** GPS/DGPS is smuggled in as the mobile fix **or** the path is RF-fingerprint training **or** **0.50 m** is silently read as absolute / the patent bar / p90 / commercial-1PPS **or** patent simultaneous-via-DGPS-1PPS is read as commercial 1PPS; succeed later would need the named bar under the lock **and** must not silently drop the honesty locks → last check: 2026-09-05 Operator **A2 Soften (conditional)**; **A1 Soften** carried → status: **open** (Amb spine = published abstract; **A1 Soften** + **A2 Soften (conditional)**; next **A3 → A4**; **not** claim clearance; hardware **X PARKED**; write-up **HOLD send** until suite digests)

## Abstract-spine leftovers (Rank-1 locked)

`A1 ref-floor honesty (absolute vs relative; DGPS ~0.4–0.5 m floor vs ideal-known-refs sim)` → kill vs soften/harden: a cheap check shows the 0.50 m sim bar was always relative under perfect refs, and absolute is floored by named DGPS, vs we keep treating 0.50 m as if it were absolute / the patent bar → last check: 2026-09-05 Operator **ADMIT Soften** — Chan abs **0.691 / 0.832** at `σ_ref` 0.4 / 0.5; abs ≤1 m poseable; abs ≤0.50 m **not**; JOINT1 scrape **0.449** rides floor — do **not** Harden absolute **X**; silent GEOM0 abs-≤0.50 **Softened** → status: **killed** / **Soften** (scored; **not** claim clearance)

`A2 clock-count / TDOA-resolution honesty (residual sync vs commercial 1PPS; abstract high-speed receiver clock; distinct from inter-ref SYNC/JOINT/DRIFT/GATE)` → kill vs soften: commercial 1PPS / public residuals blow both bars with no named window vs a named differential relative-clock window keeps the sim bar poseable (Kill not a full-string kill) → last check: 2026-09-05 Operator **ADMIT Soften (conditional)** — JOINT1 + differential ~2.5 ns (F9T-class) path-shared relative-clock named; Kill bare Chan ~1.14 m @ ~2.5 ns; Kill commodity 20–50 ns / common-view ~10 ns / absolute-only ~5–15 ns for both curves; Harden unsupported (public residuals ns–tens-of-ns, not ≪0.3 ns); brutal lock patent simultaneous-via-DGPS-1PPS ≠ commercial 1PPS → status: **killed** / **Soften** (conditional; scored; **not** claim clearance)

`A3 indoor / first-arrival / denied-box radio (GPS-denied buildings; not our additive mild-NLOS Soften)` → kill vs soften: indoor first-arrival leftover blows the patent-facing bar with no named window vs a named window keeps it poseable → last check: 2026-09-05 named; **opened** by user override suite after A2 → status: **open** (Rank-1 next)

`A4 realtime / central-compute / motion (abstract realtime as they move; our JOINT1/DRIFT1 wins were path-batch)` → kill vs soften: batch-only cannot pose the abstract’s realtime leftover vs a named realtime window is poseable → last check: 2026-09-05 named; **HOLD** until after A3 → status: **paused** / **HOLD**

`link/map GIS/CAD overlay` → kill vs succeed: overlay required before any bar can be named vs stay parked while live leftovers are A1–A4 → last check: 2026-09-05 Operator **PARK** → status: **paused** / **PARKED**

## Geometry leftover (#0)

`planar TDOA geometry bottleneck (frozen Chan 1994 2D WLS; ideal simultaneous sync + Gaussian Δt only)` → kill vs harden: geometry blows the median off the `c · σ_t` scale vs median tracks `c · σ_t` with a frozen Chan estimator → last check: 2026-09-05 Operator **ADMIT HARDEN** — 1 ns median **0.361 m** / p90 ≈ **1.16 m**; 3 ns median **1.081 m**; 0 failures; geometry is **not** the bottleneck under those idealizations; **X is median-not-p90**; ingest **HARDEN stands** → status: **hardened**

## Honest-fog lines

`spectrum / hardware vs sim-only` → kill vs succeed: a hardware **X** is required and no laptop-feasible sim/prototype path can be posed → sim-only (or hardware) must be named before **X**; a laptop-feasible sim/prototype path is poseable from public refs / assumptions → last check: 2026-09-05 **C1 SUCCEED** then **#0** locked **sim X = 0.50 m** (now scoped; not patent promise); hardware **not** required to name a sim **X** → status: **hardened** (sim-only named)

`hardware X` → kill vs succeed: a later gate names a hardware / spectrum path that can carry its own **X** vs stay parked while the live path is sim-only → last check: 2026-09-05 Operator **PARK** hardware **X** (abstract ingest does **not** unpark it) → status: **paused** / **PARKED**

`clock resolution / simultaneous-sync assumption` → kill vs succeed: no public-ref / assumption statement can even pose ≥3-ref simultaneous-sync TDOA → **fail closed** or park; a named clock/sync assumption makes the estimator class poseable (still not a locator) → last check: 2026-09-05 **C2 SUCCEED** (story named) then **#0 assumed** that ideal simultaneous sync — leftover honesty about **imperfection** was later SYNC1 / JOINT1 / DRIFT1 (now partial evidence); A2 is the abstract’s clock-count leftover → status: **hardened** (clock story named; still not a locator)

`sync-imperfection (SYNC1 Chan-alone)` → kill vs soften: injected clock / sync error blows the 0.50 m sim bar with no surviving named window vs a named near-ideal window keeps the bar (Kill not triggered) → last check: 2026-09-05 Operator **ADMIT Soften** — `σ_sync≲0.3 ns` median **0.382 m** ≤ X; later JOINT1 / DRIFT1; ingest reclassifies as **sync-fragility evidence (partial)** → status: **killed** / **Soften** (Chan-alone window; **partial** evidence)

`sync-imperfection (JOINT1 path-shared)` → kill vs soften: path-shared shared-τ fails to restore X under any named fixed_trial window vs a named window keeps the bar (Kill not triggered) → last check: 2026-09-05 Operator **ADMIT Soften** (Aim A **partial**) — fixed_trial `σ_sync ≲ 3 ns` restores X; ingest reclassifies as **partial** sync-fragility evidence → status: **killed** / **Soften** (fixed offsets; **partial** evidence)

`clock drift vs shared-τ (DRIFT1)` → kill vs harden: a later drift pulse shows whether a ramp / path-drift model can keep the 0.50 m bar vs shared-τ stays misspecified → last check: 2026-09-05 Operator **ADMIT HARDEN** — batch path-shared τ + linear α; drift=3 @ `σ=0` **0.221 m**; drift=10 **0.223 m**; **α̂ recovers**; honesty = path-shared **batch**, **not** free per-epoch realtime; ingest: still **partial** sync-fragility evidence (not A4 realtime) → status: **hardened** (scored under named budget; **partial** vs patent set)

`refuse-belt (GATE1)` → kill vs soften: a detect-only residual∨LORO belt either blows in-band FA or misses out-of-budget cases vs a named belt keeps FA≈0.10 and high TD (Kill not triggered) → last check: 2026-09-05 Operator **ADMIT Soften** (aim B Succeed) — FA `σ≤3` drift0 ≈ **0.100**; TD σ=10 ≈ **0.828**; ingest: **partial** sync-fragility evidence → status: **killed** / **Soften**

`multipath in a GPS-denied box` → kill vs succeed: this is standing honesty, not a hunt — a denied box is a hard radio environment; do **not** silently drop multipath; Kill = 0.50 m not poseable even under LOS / mild NLOS vs Soften = 0.50 m stays with NLOS scope vs Harden = multipath-robust 0.50 m → last check: 2026-09-05 Operator **ADMIT Soften** — Kill **not** triggered; poseable under LOS + mild/intermittent NLOS; **not** A3; **later** → status: **restated** / **Soften** (NLOS-scoped; not multipath-robust; **later**)

`multipath / NLOS positive-bias injection (same frozen Chan)` → kill vs harden: inject positive NLOS / multipath bias under the **same** Chan 1994 and show whether the **median** 0.50 m bar survives → last check: 2026-09-05 Operator **ADMIT Soften** (Kill not triggered) — baseline **0.364 m**; mild/intermittent **0.476 m** / **0.452 m**; strong persistent **0.73–4.7+ m** → status: **restated** / **Soften** (scored; NLOS-scoped; **later**; **not** A3)

## First-pulse data / measurement line

`public refs + sync assumptions + measurement availability for a laptop-feasible sim/prototype path` → kill vs succeed: no usable public refs / assumptions / measurement story → **DATA-BLOCKED park** or sim-only Soften (must be said); a citable public-ref / assumption peek that names fog and whether **X** is sim-geometry vs hardware → peek succeed (**not** claim clearance) → last check: 2026-09-05 fog naming **ADMITTED** — C1/C2/C3 **SUCCEED**; **X** class = sim-geometry first → status: **killed** / **PASS** (peek succeed; **not** claim clearance)

## Scored pulses

`#0 geometry-bottleneck sim (GEOM0)` → kill vs succeed: a laptop, no-RF sim using **frozen textbook multilateration only** that can freeze a numeric sim **X** vs a pose that still cannot score a held-out path → last check: 2026-09-05 Operator **ADMIT HARDEN** — Chan 1994 2D WLS; numeric **sim X = 0.50 m** (**median**-based @ 1 ns; **median-not-p90**; 1 ns p90 ≈ **1.16 m**); geometry not the bottleneck under **perfect refs**; later **A1 Soften** names that 0.50 m as **perfect-ref scoped sim only** (silent absolute-≤0.50 **Softened**) → status: **hardened** (scored; **not** claim clearance; **not** patent promise; **not** absolute under RN floor)

`SYNC1 sync-imperfection` → kill vs soften: loose sync / path drift kills the 0.50 m bar with no named window vs a near-ideal window keeps the bar → last check: 2026-09-05 Operator **ADMIT Soften**; ingest: **partial** sync-fragility evidence → status: **killed** / **Soften** (scored; Chan-alone window; **partial**)

`JOINT1 path-shared joint clocks` → kill vs soften: shared-τ fails to restore X under any named fixed_trial window vs a named window keeps the bar → last check: 2026-09-05 Operator **ADMIT Soften** (Aim A **partial**); ingest: **partial** → status: **killed** / **Soften** (scored; **fixed offsets**; **partial**)

`MULTIPATH1 multipath-bias` → kill vs soften: any NLOS kills the 0.50 m bar vs a named mild window keeps it → last check: 2026-09-05 Operator **MULTIPATH1 Soften** — LOS + mild/intermittent NLOS only → status: **restated** / **Soften** (prior; **not** claim clearance; **later**; **not** A3)

`DRIFT1 pulse` → kill vs harden: whether a ramp / path-drift model can keep the 0.50 m bar → last check: 2026-09-05 Operator **ADMIT HARDEN** — batch path-shared τ + linear α; ingest: **partial** sync-fragility (not A4 realtime) → status: **hardened** (scored; **not** free per-epoch realtime; **partial** vs patent set)

`GATE1 pulse` → kill vs soften: refuse belt fails to catch out-of-budget cases without blowing in-band FA vs a named detect-only belt keeps FA≈0.10 and high TD (Kill not triggered) → last check: 2026-09-05 Operator **ADMIT Soften** (aim B Succeed); ingest: **partial** → status: **killed** / **Soften** (scored; refuse belt; **not** a repair; **partial**)

`US10135667B1 abstract ingest` → kill vs admit: abstract cannot be named as Amb spine without claim-language product copy vs published abstract + bibliographic + Operator-named bars can be admitted → last check: 2026-09-05 Operator **ADMIT ingest**; copy gate **PASS** → status: **killed** / **ADMITTED** (spine named; **not** claim clearance)

`A1 ref-floor pulse` → kill vs soften: RN survey error blows patent-facing ≤1 m with no named window vs abs ≤1 m stays poseable and 0.50 m is named as perfect-ref only (Kill not triggered) → last check: 2026-09-05 Operator **ADMIT Soften** — Chan abs **0.832 m** @ `σ_ref=0.5`; **0.691 m** @ 0.4; JOINT1 scrape **0.449** rides floor; do **not** Harden absolute **X** → status: **killed** / **Soften** (scored; RN floor; **carried**; **not** claim clearance)

`A2 residual-sync vs commercial 1PPS` → kill vs soften: commercial 1PPS / public residuals blow both bars with no named window vs JOINT1 + ~2.5 ns F9T-class path-shared relative-clock keeps the sim bar (Kill not a full-string kill) → last check: 2026-09-05 Operator **ADMIT Soften (conditional)** — Kill bare Chan ~1.14 m; Kill commodity / common-view / absolute-only on both curves; Harden unsupported; brutal lock → status: **killed** / **Soften** (conditional; scored; **not** claim clearance)
