# Digestion — from the llm-gwt remnant string

A short plain note before this new claim. Habit: [`docs/DIGESTION_HABIT.md`](../../docs/DIGESTION_HABIT.md). This does **not** score Collatz. It does **not** reopen the last string.

**Last string:** live remnant of `2026-08_llm-global-workspace-consciousness` (R-SOME / R-RECIPE locks, then R-REPL invent).

## What it taught

Definition locks can **outrun infra**.

- **R1** needed model **weights** (local mech). The lock named a check the machine could not run.
- An **API** could not fake those insides. Behavior-over-API is not the locked R1 measurement.
- **Credential** runs were **parked**. Do not chase GPU, Hugging Face tokens, or keys we do not have.

Infra-kill is **not** a hallmark fail. Parking credentials is **not** clearance.

## What this Collatz string must do

Stay **runnable on ordinary CPU**. No GPU. No weights. No API keys.

If a proposed shortcut needs special hardware or a paid/credentialed service, it does not belong on this string.

## What stays parked

- **llm-gwt R-REPL** stays **paused** (C1 infra-kill; do not chase access).
- **Track B** invent stays **paused** (oil spot string; digest already written).

This note does **not** authorize those strings. It does **not** prove Collatz.
