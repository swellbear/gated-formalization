# Named-gap ledger — Collatz shortcut map

Habit: [`docs/NAMED_GAP_LEDGER_HABIT.md`](../../docs/NAMED_GAP_LEDGER_HABIT.md). One line per open gap. This is a problem-solving scoreboard, **not** a proof.

**Opened:** 2026-09-05 — Founder opens the **Collatz shortcut-map** leashed invent→test string. Lab invents ranked shortcuts. Operator gates.

**Last check:** playground close — **K1** / **K2** / **K3** gated; control fix standing; digestion written. **Playground invent complete. Lab HOLD.**

**What this is not:** Collatz is **not** proved. K1/K2 are **not** new dynamics. K3 is **not** novelty. This is **not** “beats random affine.” Hunt scripts are **not** merged.

**Process:** Lab invents 2–3 ranked options for this named gap (why / cost / kill-vs-harden). Operator admits, rejects, or parks. Lab does **not** self-admit. Invent→test: [`docs/INVENT_TEST_HABIT.md`](../../docs/INVENT_TEST_HABIT.md). After this fold Lab does **not** invent further on this playground.

**Run constraint:** ordinary CPU only — no GPU, no weights, no API keys.

**Track B invent** remains **paused** (unchanged).  
**llm-gwt R-REPL** remains **paused** (unchanged).

## Lines

`shortcut reducing average stopping time / steps-to-1 on a finite pre-registered range vs baseline + control` → kill vs harden: a cheap pre-registered check that would drop the shortcut (no average-step reduction vs baseline and control) vs only tighten the finite-range comparison → last check: **K1** T1 ratio 0.6681; **K2** T1 ratio 0.3378 (both 0 fail vs C) → status: **hardened-vs-C** (orbit compression only) / playground invent **complete**

`Amb remainder — vs C is Collatz-orbit compression (K1 forced halves; K2 max 2-batching); random affine is not a faithful reach-1 control` → kill vs harden: a distinct reach-1 control (not identical to the candidate; not a timeout-heavy affine) vs only restating the T1 vs-C ratio → last check: Control-A `(3,1)` identical to K1 (thin; now permanently excluded); Control-B all pool fail smoke / `ok_frac≈0.008`; Control-C 9999/9999 fail → status: **wrong-piece** / **paused** (affine-control limb; see [`CONTROL_FIX.md`](CONTROL_FIX.md))

`K2 vs same-orbit even strip-1 (Control-B-orbit)` → kill vs harden: drop max-2-batching if it does not beat strip-1 vs only a thin count difference → last check: K2 28.705 ≤ 29.205 → status: **hardened** (THIN Amb bite; not “beats random affine”)

`K3 mod-16 table novelty` → kill vs harden: a mismatch vs K2-step that would show a new map vs only a lookup packaging → last check: T1 mean 28.705 identical; 0 mismatches vs K2-step; vs Syracuse 28.705 ≤ 56.770 is restatement of K2>K1; C-limb HARDEN-TAG = restatement of K2 only → status: **paused** (novelty **parked**; packaging not invent)

`Playground invent string` → kill vs harden: further ranked shortcuts (K4+) vs stop and digest → last check: K1/K2/K3 gated; digestion [`DIGESTION_Collatz_playground_2026-09-05.md`](DIGESTION_Collatz_playground_2026-09-05.md) → status: **paused** (complete; Lab **HOLD** for Founder real-problem discussion)
