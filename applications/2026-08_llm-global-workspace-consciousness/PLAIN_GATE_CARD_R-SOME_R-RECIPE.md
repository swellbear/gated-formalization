# Plain gate card — R-SOME / R-RECIPE (exploration A)

**Application:** `2026-08_llm-global-workspace-consciousness`  
**Date / checkpoint:** 2026-09-05 Operator gate + sub-lock gate + Founder addendum  
**Ledger:** [`NAMED_GAP_LEDGER.md`](NAMED_GAP_LEDGER.md)  
**Lab boards:** [`PROPOSED_Lab_R-SOME_R-RECIPE_options.md`](PROPOSED_Lab_R-SOME_R-RECIPE_options.md) · [`PROPOSED_PENDING_SUBLOCKS.md`](PROPOSED_PENDING_SUBLOCKS.md)

---

## What we’re doing

Recording a **definitional** lock so “some models” and “the hallmark recipe” are named enough to check later. The three pending sub-locks are now **cleared**. This is **not** a proof that anything is conscious.

## What we need from you

None — informing only. **R-REPL** is no longer definition-blocked, but it is **not authorized** to invent or run until Founder / Operator opens that string.

## What a “yes” / this update means

Meanings are named. Ambiguity is tighter because the remnant is checkable. That is **not** clearance.

## What this does *not* mean

Consciousness is **not** proved. This is **not** an Admissible-band elevation. **A-Strong**, sufficiency, unconstrained scale, and Strong-GNW stay parked. Cleared sub-locks do **not** start R-REPL.

---

## What each option is (one everyday sentence each)

- **S1** — “some” = models named in the admitted Gurnee writeup (plus later copies that pass a cite check)
- **S2** — “some” for re-measurement = a short pre-picked list of open-weight models we can actually run tests on
- **S3** — “some” = any model bigger than a size cutoff (**rejected**)
- **R1** — measure three workspace hallmarks on the model’s insides (talk-ready subspace, hurts flexible thinking more than automatic, broader influence); pass if ≥2/3
- **R2** — cheaper behavior-only checklist (no insides); optional screen, not the main recipe
- **R3** — Ignition Index β̂ score as a side check only — **not** allowed as the Weak-hallmark pass

## Why admit / reject (plain)

- **ADMIT S1+S2:** ties “some” to named evidence and to models we can re-test; **REJECT S3:** size cutoff smuggles scale Amb
- **ADMIT R1** as main recipe + **R2** as optional screen; **REJECT R3** as Weak pass: wrong object (dynamical Strong companion, not Weak hallmarks)

## Exact pendings — now **cleared**

1. **Qwen cite audit — cleared.** `Qwen/Qwen3.6-27B` enters S1(b) as **PASS_WITH_CAVEATS** (Bhatia/Blank/Nanda AF/LW 2026-07-20). Caveats stay on the ledger: AF not journal; hybrid DeltaNet+attn; n=25≪1000. Same operational hallmark class.
2. **S2 Hugging Face IDs — cleared / LOCKED.** Default: `Qwen/Qwen3.6-27B`, `google/gemma-2-27b-it`, `meta-llama/Llama-3.1-8B-Instruct`. Optional later only: `meta-llama/Llama-3.3-70B-Instruct` (not in the default lock).
3. **H-thresholds — cleared / FROZEN.** Thresholds frozen to the **numeric rules** in [`PROPOSED_PENDING_SUBLOCKS.md`](PROPOSED_PENDING_SUBLOCKS.md) §3 (not kinship gloss). Pass = ≥2/3. R2 optional. R3 not a Weak pass.

---

## Details (technical record — secondary)

| Item | Value |
|------|--------|
| Claim under test | Narrowed A-Weak remnant only (`00_Working_Claim_Branch_A.md`) |
| Amb | ≈ 2.5 (definitional lock ≠ clearance) |
| Locks | **ADMIT S1+S2** (S3 rejected); **ADMIT R1+R2** (R3 not Weak pass); sub-locks **COMPLETE** |
| R-REPL | Open-but-**not-authorized** / park-until-trigger |
| Verdict | Still **not** consciousness proved; **not** Admissible elevation; **not** A-Strong |

---

*Glossary: `docs/READER_GLOSSARY.md`.*
