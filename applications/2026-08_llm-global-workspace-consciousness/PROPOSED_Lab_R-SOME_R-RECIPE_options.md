# PROPOSED Lab board — R-SOME / R-RECIPE options (on record)

**Date:** 2026-09-05  
**Application:** `2026-08_llm-global-workspace-consciousness`  
**String:** exploration A — live remnant only  
**Habit:** [`docs/NAMED_GAP_LEDGER_HABIT.md`](../../docs/NAMED_GAP_LEDGER_HABIT.md) · [`docs/SOLVE_LOOP_HABIT.md`](../../docs/SOLVE_LOOP_HABIT.md)  
**Plain card:** [`PLAIN_GATE_CARD_R-SOME_R-RECIPE.md`](PLAIN_GATE_CARD_R-SOME_R-RECIPE.md)

Lab invents. Lab does **not** self-admit. This file is the **PROPOSED** board plus the **Operator gate** after it landed.

**What this is not:** Consciousness is **not** proved. This is **not** an Admissible-band elevation. **A-Strong**, sufficiency, unconstrained scale, and Strong-GNW stay parked.

---

## R-SOME — named set of models that count as “some”

| Rank | Option | Everyday meaning | Why it could clear this fog | Rough cost | Kill vs harden |
|------|--------|------------------|-----------------------------|------------|----------------|
| 1 | **S1** | “some” = models with Gurnee-class J-space / verbalizable-workspace hallmarks in admitted primary material (seed: Claude Sonnet 4.5) ∪ later independent same-hallmark copies after a cite audit (`Qwen/Qwen3.6-27B` now **PASS_WITH_CAVEATS**) | Ties the word “some” to named admitted evidence, not a slogan | Cheap cite / writeup check | Kill if no named model can be pointed at; harden if the named set is just tightened |
| 2 | **S2** | Pre-registered open-weight cohort = R-REPL / re-measurement targets (HF IDs now **LOCKED**) | Names models we can actually re-run a recipe on | Cheap list + later run cost | Kill if no runnable cohort exists; harden if IDs are only pending |
| 3 | **S3** | Size-band (≥N) — any model bigger than a cutoff | Looks cheap, but smuggles scale Amb into “some” | Cheap number | **REJECT** — scale-smuggle; not a named set |

### Operator gate (R-SOME)

- **ADMIT S1+S2** as the definitional lock for “some.”
- **REJECT S3.**
- **Sub-locks (later same day):** Qwen cite audit **PASS_WITH_CAVEATS**; S2 HF IDs **LOCKED**. See [`PROPOSED_PENDING_SUBLOCKS.md`](PROPOSED_PENDING_SUBLOCKS.md).

---

## R-RECIPE — cheap operational hallmark recipe we can measure

| Rank | Option | Everyday meaning | Why it could clear this fog | Rough cost | Kill vs harden |
|------|--------|------------------|-----------------------------|------------|----------------|
| 1 | **R1** | Three inside-the-model hallmarks: **H-verb** (talk-ready subspace), **H-sel** (hurts flexible thinking more than automatic), **H-bcast** (broader influence). Pass = ≥2/3 at **FROZEN** thresholds ([`PROPOSED_PENDING_SUBLOCKS.md`](PROPOSED_PENDING_SUBLOCKS.md) §3) | Makes “partial Weak hallmarks” a checkable recipe, not a vibe | Medium once thresholds freeze | Kill if the three hallmarks cannot be stated; harden if only the numbers are still open |
| 2 | **R2** | Optional cheap behavior-only screen (no insides) | A cheap filter — **not** a substitute for R1 | Cheap | Harden as a screen only; kill-as-main-recipe if used instead of R1 |
| 3 | **R3** | Ignition Index β̂ as the Weak pass | Wrong object: dynamical Strong companion / negative-control, not Weak hallmarks | Medium | **REJECT as Weak-pass.** May stay companion / negative-control only |

### Operator gate (R-RECIPE)

- **ADMIT R1** as the **primary** recipe.
- **ADMIT R2** as an **optional screen only** — not a substitute for R1.
- **REJECT R3-as-Weak-pass.** R3 may remain a companion / negative-control only.
- **Sub-locks (later same day):** H-thresholds **FROZEN** as [`PROPOSED_PENDING_SUBLOCKS.md`](PROPOSED_PENDING_SUBLOCKS.md) §3.

---

## After the sub-lock gate

R-SOME + R-RECIPE definitional locks are **COMPLETE**. **Later (2026-09-05):** Founder opens **R-REPL** invent; Operator **C1 KILL-unrunnable (infra)**; API A3/A1/A2 gated; Founder parks credentialed runs (see [`NAMED_GAP_LEDGER.md`](NAMED_GAP_LEDGER.md)). Remnant only. R-REPL **paused**.

Track B invent remains **paused** (unchanged).
