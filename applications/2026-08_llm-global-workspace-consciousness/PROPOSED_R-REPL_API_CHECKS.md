# PROPOSED R-REPL API options — Lab board (on record)

**Date:** 2026-09-05  
**Application:** `2026-08_llm-global-workspace-consciousness`  
**String:** exploration A — live remnant only  
**Named gap:** On locked R-SOME (S1+S2), does locked R-RECIPE (R1) show partial Weak hallmarks (or fail honestly)?  
**Local sibling:** [`PROPOSED_R-REPL_CHECKS.md`](PROPOSED_R-REPL_CHECKS.md) · [`C1_SCORE_PROPOSED.md`](C1_SCORE_PROPOSED.md)  
**Ledger:** [`NAMED_GAP_LEDGER.md`](NAMED_GAP_LEDGER.md)

Lab invents 2–3 ranked API-feasible options. Lab does **not** self-admit. This file is the **PROPOSED** API board plus the **Operator gate** and **Founder/user disposition** after they landed.

Lab scratch path for this board was **not** on this fold VM. Options below are the gated set (A1 / A2 / A3) as Operator-named. No extra unpublished API recipes, no workarounds, and no token scrape are invented.

**What this is not:** Consciousness is **not** proved. This is **not** an Admissible-band elevation. **A-Strong**, sufficiency, unconstrained scale, and Strong-GNW stay parked. This is **not** an R1 pass via API.

---

## Ranked API options

| Rank | Option | Everyday meaning | Why it could clear this fog | Rough cost | Kill vs harden |
|------|--------|------------------|-----------------------------|------------|----------------|
| 1 | **A1** | Optional R2 behavior-only screen over a chat API (no insides; **not** a substitute for R1) | May tighten **behavioral / partial** Amb only | Cheap **if** the user already has keys and later volunteers them | Harden as a screen only; kill-as-main-recipe if used as R1 / remnant clearance |
| 2 | **A2** | Standalone API closer — treat an API-only readout as enough to pass or fail the remnant | Looks cheap; smuggles locked R1 / mechanistic privilege | Cheap | **REJECT as standalone** |
| 3 | **A3** | Standing honesty: locked R1 mech **cannot** be run over API | Names the remainder instead of pretending the API path is R1 | Free | Kill “API cleared remnant”; harden remainder as parked mech Amb on S2 |

---

## Operator gate (API redesign)

- **A3 ADMIT** as standing honesty: locked R1 mech is **impossible** over API. Remnant Amb on **mechanistic privilege on S2** stays open/parked until GPU + HF **or** Founder demotes the elevation bar to R2 (**not** demoted here).
- **A1 ADMIT** as an **optional non-elevating R2 behavioral screen only**. Not R1. Not remnant clearance. Not a Weak-hallmark pass.
- **A2 REJECT** as standalone.

API path may only ever clear **behavioral / partial** Amb. Full R1 still needs weights.

---

## Founder/user disposition (same day)

**Do not pursue access we don’t have.** Default = park runs that need new credentials.

- Local-GPU C1: KILL-unrunnable (infra) — **paused** (not hallmark fail).
- R1 mech on S2: **parked** (needs weights; A3 honesty).
- A1 R2 API screen: **parked** until the user **voluntarily** provisions keys they **already own**. Do **not** chase keys.
- No R2 demotion of the elevation bar.
- No workarounds. No token scrape.
- Lab **HOLD** on runs.
- Track B invent remains **paused**.
- Remnant Amb remains **open/parked honestly**.

---

## After this gate

R-SOME + R-RECIPE definitional locks stay. R-REPL local runnability is a **wrong-piece** (C1 infra). Mechanistic remnant check is **parked**. Optional A1 screen is **parked** (keys). This is **not** consciousness proved. **Not** Admissible. **Not** A-Strong. **Not** an R1 pass via API.

---

*Docs only. Lab does not self-admit. Not consciousness proved. Not Admissible. Not A-Strong.*
